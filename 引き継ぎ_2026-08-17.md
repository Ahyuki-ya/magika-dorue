# セッション引き継ぎ（2026-08-17）

Magika・Dorue（単一HTML・依存なし・git化済み）の開発セッション引き継ぎ。
正典ファイルは [index.html](index.html) 一本。詳細仕様は [magi_CLAUDE.md](magi_CLAUDE.md)
（`### 異種交配（ハイブリッド）` が今回分）。

起点コミット `943f0e5` → **`ba964a8`（元からあった未コミット作業）→ `d9c075c`（今回の主題）**。
ブランチは `feat/license-billing`。**push はしていない。**

**今回の主題は「異種交配（ハイブリッド）システム」。** 従来の子孫システムは同種のみで成立していた
（繁殖ループが種族ごとに回り、その中で `m.mtype === mt` でフィルタするので構造的に異種ペアが成立しない）。
これをダイヤのスキルツリーで解放し、異なる種族どうしが交配して両親のいいところを併せ持つ
新モンスターが生まれるようにした。

---

## ユーザーが決めた分岐（迷ったらここに戻る）

1. **ステータスは「名前に矛盾しない」いいとこ取り** — 「銅ゴブリンなら、敏捷性はそのまま、
   攻撃性と耐久性が上がる。銅になるので」
2. **ハイブリッドは独自レベル** ＋ 強化パネルにカードを追加（親のレベルからの導出ではない）
3. **繁殖は同じハイブリッド同士のみ**（3種混合は発生させない）
4. **スキルツリーは方向ごとに別ノード** — 「苔ゴーレム」と「銅ゴブリン」は別々に買う
5. **交配の発生は確率**。釣り合いとして (a) fertile 供給を増やす宝具アフィックスを追加、
   (b) 方向ノードの解放で fertile 供給 +1（説明は控えめでよい）

---

## 今回やったこと

### 1. mtype を `"body+coat"` の2軸にした

| | 役割 |
|---|---|
| `body` | フォルム。**見た目の体つき・索敵距離**を決める |
| `coat` | 素材。**色・贈るステータス**を決める |

`"goblin+golem"` = 銅ゴブリン（ゴブリンの体に銅の素材）。4形 × 自分以外3素材 = `HYBRID_MTYPES` 12種、
純血と合わせて `ALL_MTYPES` 16種。`bodyOf` / `coatOf` / `isHybrid` / `hybridId` で分解・判定・組み立て。

**この設計が効いたのは `mtype` への依存が全体で22箇所しかなかったから。** `monsterLevels` /
`monsterCap` / `randBonus` / `loadMonsterCaps` は元から `ALL_MTYPES` を回していたので、
**独自レベルの器は追加コードなしで乗った**。

### 2. ステータス合成規則（本改修の核）

`rebuildHybridGrowth(deep)` が `MONSTER_GROWTH[body]` と `[coat]` から合成する。
**素材が「贈る」ステータス（`MAT_GIFT[coat]`）だけ両者の max を取り、他と `range` は体のまま。**

| | 攻 base/slope | 敏 base/slope | HP base/slope | 索敵 |
|---|---|---|---|---|
| 苔ゴブリン（純血） | 1 / 0.6 | 2 / 1.0 | 2 / 0.4 | 10 |
| **銅ゴブリン** `goblin+golem` | **2 / 0.6 ↑** | 2 / 1.0 **そのまま** | **4 / 1.4 ↑↑** | 10 |
| 銅ゴーレム（純血） | 2 / 0.5 | 1 / 0.2 | 4 / 1.4 | 4 |
| **苔ゴーレム** `golem+goblin` | 2 / 0.5 → | **2 / 1.0 ↑↑** | 4 / 1.4 → | 4 |

体と素材を入れ替えると贈り物の組が変わるので**非対称**（索敵10と4）。単純な max だと対称になって
「銅ゴブリン＝苔ゴーレム」になってしまうので、`MAT_GIFT` で贈るものを絞るのが要。

### 3. 交配は「繁殖が成立した瞬間に振り替える」方式

**毎フレーム確率を振るのではなく、同種繁殖の条件が揃った瞬間に1回だけ振って結果をハイブリッドに
振り替える。** `breedData.root` が偽なら判定コードに入らないので **`Math.random()` を1回も呼ばない**。

これは**設計上の要**。Phase 5 の再現性テストは乱数消費回数でハッシュを取るので、毎フレーム振る実装だと
「異種交配を実装したら既存の挙動が変わった」ことになり、回帰の砦を失う。実際、**6フェーズを通して
`path: bf386fb2` / `sim: 3ceb1848`・rngCount 437 が1ビットも動いていない。**

### 4. スキルツリー（`magika_breed`）

ショップとゲーム内ドロワーの両方に 🧬 交配タブ。ルート（💎30・累計繁殖50）→ 方向ノード12（💎15〜30・
影が絡む6個は `shopData.wraith` が前提）→ 強化枝3（💞番いの導き＝交配確率 30/45/60% ／
🍼混血の繁栄＝ハイブリッド専用のちび上限 ／ 🧬深き継承＝`MAT_GIFT2` を追加適用・3種解放が前提）。

方向ノードの価格は `breedNodeCost(mt)` が贈り物の強さで決める（銅は攻とHPの2つを贈るので高い、
影はさらに高い、体がレイスだと索敵最長のため加算）。

### 5. 描画

体は **body の種族の描画コードをそのまま流用**し、その上に素材を4段で重ねる
（素材色を `globalAlpha 0.38` でひと塗り → 素材色の輪郭線 → 素材アクセント）。
アクセントは 石=角ばった粒／苔=緑の斑点／銅=金のボルト／影=紫のもや。
**`shadowBlur` 0件・グラデーション生成0件を全16通りで実測確認済み**（描画性能予算）。

`traceFormSilhouette` は体の輪郭の**パスだけ**作る小関数。HPバーは体の上方に描かれ、
シルエットは体の輪郭だけなので、**バーは素材色に染まらない**（全フォルムで 2.75〜4px の隙間、
影のもや `rad×1.4` でも 1.0〜1.8px の余裕がある。幾何で確認済み）。

### 6. その他

- 強化パネルは `syncHybridLevelCards()` が解放済みハイブリッドのカードを生成する。
  **id を手書きカードと揃えたので `updateLevelPanel()` は無改造で動く**（手書き4枚は触っていない）
- `epilogue_cap.js` を修正。期待値を `5` / `6` と直書きしていたせいで、初期上限 5→20・
  1💎あたり +1→+5 に変えたときに取り残されていた。**定数から導く形にした**

---

## 実装中に判明したこと（同じ轍を踏まないため）

### ① `fertile` の供給と消費は、また釣り合わなかった

前セッションの教訓③（`fertile` は供給と消費が非対称）の再来。当初は「同種繁殖を先に回して、
余った fertile で異種交配する」設計にしていたが、**現行の供給は勇者登場ごとに各種ちょうど2体で、
同種繁殖がちょうど2体使い切るので余りが出ない** ＝ 異種交配が一度も発生しない。

→ 発生を**確率**にし、供給側を3方向で増やした（方向ノードの隠れ特典 +1 ／ 新アフィックス
「繁殖の祝福」／ 混血の繁栄でハイブリッド専用の上限）。
**`fertile` を触るときは必ず供給と消費が釣り合うか確認する**、は今回も有効だった。

### ② `activeMtypes()` の**配列長**が乱数消費に直結する

`index.html` の `activeMtypes().slice().sort(() => Math.random() - 0.5)` は、
**配列長ぶんの乱数を消費する**。ここに解放済みハイブリッドを足すと黄金ハッシュが変わる。
→ `unlockedHybrids()` が空のときは `pure` を**そのまま**返す形にした（`concat` しない）。
**`activeMtypes()` の戻り値の長さを変える改修は、必ず `verify.sh` で裏を取ること。**

### ③ ハーネスの DOM スタブは **id を自動生成する**

`test/p5/harness.js` の `makeEl()` は Proxy なので、**存在しない id でも要素を返す**。
つまり「自分のコードが要素を作ったか」をハーネスでは検証できない（常に true になる）。
→ DOM 生成の検証が要るときは、**null を返す厳密なスタブを別に用意する**。
`epilogue_hybrid.js` の項目9は割り切って「例外が出ないこと」だけを見ている。

### ④ `AFFIX_TYPES` への追加は宝具抽選の乱数消費を変える

新アフィックスを足すと重み付き抽選のプールが変わる。今回は `forge` / `kind` が閾値ベースだったので
落ちなかった（`kind` の実測%だけ動いた）が、**期待値を直書きしているテストがあれば落ちる**。

### ⑤ 巨大な単一ファイルでは TDZ が刺さる

`BREED_NODES` を `SHOP_ITEMS` の直後に `const` で置くと、tier1 の生成が約1,800行後で宣言される
`HYBRID_MTYPES` を参照して**起動時に `ReferenceError` で落ちる**。
→ データ生成を**関数宣言**（ホイスティングされる）にして、実体化の1行だけを依存が揃う位置に置いた。
**単一 `<script>` の上の方に定義を足すときは、参照先の宣言位置を必ず確認する。**

---

## ⚠ 未検証：バランス

**ハイブリッドを解放した状態での実走の計測を一度もしていない。** これが最大の穴。

設計上は「純血の上位互換だが、掘削では出ない＝交配限定という希少性」「ダイヤの投資」
「独自レベルをゼロから育てる手間」で釣り合わせるつもりだが、**数字で確かめていない**。
特に懸念:

- **銅レイス** `wraith+golem` は攻撃 base3/slope1.2 に HP base4/slope1.4 と索敵12 が乗る。
  ガラスキャノンが硬くなるので、終盤の壊れ性能になりうる
- **シャドウゴーレム** `golem+wraith` は硬いまま攻撃 slope が 0.5→1.2 になる
- 交配確率60%＋fertile供給の増加で、**繁殖の総量そのものが増えている**可能性
- ハイブリッド専用のちび上限（混血の繁栄）が、実質的な軍拡になっていないか

計測は `epilogue_colosseum.js` の実走シナリオか `epilogue_balance` / `epilogue_tempo` の
ものさしに、`breedData` を解放済みにした条件を足すのが早い。

---

## 次にやること

### A. 異種交配の残り（優先）
1. **上のバランス計測**。特に銅レイス／シャドウゴーレムの終盤性能
2. **交配の発生頻度の実測** — 確率30%が体感でどのくらいか。少なすぎると解放の実感がない
3. **見た目の実プレイ確認** — 静止画では16通りとも読めたが、30pxタイルで動いている中で
   体と素材が判別できるかは未確認
4. **深き継承（💎40）の効果が地味な組がある** — `MAT_GIFT2` は max を取るので、
   元から体の方が高いステータスだと何も動かない（例: 銅ゴブリンの敏捷）。買う前に分かるようにするか、
   贈り方を変えるか

### B. 前セッションからの持ち越し（未着手）
5. **hard モードの「連続流入」化**
6. **早送りで城が落ちる問題**（`CASTLE_MAX_HP` 40→120 案・ユーザー判断で保留中）
7. **宝具の時価売却**（Phase 2 の残り）
8. **道B オンライン市場**（要バックエンド）
9. [宝具リスト.md](宝具リスト.md) への名前追加

### C. ドキュメントの負債
10. `magi_CLAUDE.md` の Breeding System 節に **`BREED_MAX = 8` と書いてあるが実際は
    `8 + shopData.breedCap * 2 + treasureBreedCap`**。今回は指示どおり触らなかった
11. `magi_CLAUDE.md:502` に記載のある `resetCarryOver()` は**現行 index.html に存在しない**

---

## 検証の作法（触る前に読む）

```sh
test/p5/verify.sh index.html          # path/sim の黄金ハッシュ照合（コミット前に必ず）
node test/p5/harness.js index.html <mode>
```

| mode | 用途 |
|---|---|
| `hybrid` | **53項目・今回追加**。名前／合成規則／深き継承／即時反映／スキルツリー／交配の発生／fertile供給／旧セーブ互換／UIスモーク |
| `colosseum` | 69項目。5b〜5d（繁殖が掘削の枠を食わない）と 5q/5r（fertile上限）が繁殖まわりの砦 |
| `affix` | `dynamicEquip` / `dynamicLevel`＝**強化は即反映**の砦 |
| `design` | 描画の性能予算とUIの土台。項目11 が `dev/screens.html` の呼ぶ名前とidを検査 |
| `cap` | レベル上限。**期待値は定数から導いてある**（直書きに戻さないこと） |
| `path` / `sim` | 経路探索・全軌道の等価性 |

**現行の黄金値: `path: bf386fb2`（18,844サンプル・`BASE_COLS=19`）／`sim: 3ceb1848`・rngCount 437。**
異種交配は未解放時にこれを1ビットも動かさない設計なので、**動いたら実装が壊れている合図。**

### 設計上の不変条件（崩さない）
- 描画は2階調ベタ塗り。**毎フレームのグラデ生成と `ctx.shadowBlur` は禁止**
- 経路探索は**深度非依存**を壊さない
- 強化（Lv・宝具）は**常に即反映**。ちびは成体の半分、HPは割合維持
- **異種交配は未解放時に乱数を消費しない**（← 今回追加。再現性テストの前提）
- **プレイヤーの意思決定（掘削）と自動処理（繁殖）は枠を分ける**
