# デザイン刷新 — 発注書（2026-09-09）

Magika・Dorue の UI から「AI が作った感」を抜き、世界観を1本に通すための発注書。
**4本に分けてある。番号順に出すこと**（1が以降すべての前提になる）。

---

## 0. 診断（なぜこの4本なのか）

現状の CSS は悪くない。[index.html:12-33](index.html#L12-L33) にデザイントークンがあり、
間隔は 4px 刻み、角丸は `--radius: 2px` で統一、「生の色コード直書きをしない」という規約もある。
**レイアウトと設計は問題ではない。** 問題は絵柄の3点に集中している。

| # | 症状 | 実測 |
|---|---|---|
| ① | **絵文字を UI アイコンに使っている** | **83種類・354箇所**（CSS 1 / HTML 96 / JS 240）。OS ごとに絵が変わる＝色も線幅もパースも制御を放棄している |
| ② | **画面に3種類のフォントが同時に出ている** | `Courier New` に**日本語グリフが無い**ため、英数字＝Courier New／日本語＝OS のゴシック／アイコン＝カラー絵文字。別々の時代・文化の書体が3つ同時に出ている |
| ③ | **「それっぽさ」を記号で足している** | [index.html:196-212](index.html#L196-L212) の星屑、[index.html:184](index.html#L184) の多層 text-shadow。統一された画づくりの結果ではなく、雰囲気の記号を後から乗せている |

**どのモデルも「アイコンを付けて」と言われれば絵文字を出す。禁止されていないから出る。**
だから最初にやるのは絵を描くことではなく、**禁止事項を規約に書くこと**（発注1）。

---

## 参照作品と、その切り分け（全発注の共通前提）

ユーザーが挙げた参照は **『勇者のくせになまいきだ。』**（PSP, 2007）。
地中の断面を掘り、魔物を育て、勇者を迎え撃つ——**本作の構造そのものの元ネタ**。
補助参照として **ドラゴンクエスト4/5**（FC/SFC）。

**この2つは視覚的に別方向なので、領域で切り分ける。**

| 領域 | 寄せる先 | 理由 |
|---|---|---|
| **盤面**（地層・モンスター・勇者・城） | **勇なま** | 掘る断面図というゲーム構造が同じ。ここが世界観の主役 |
| **UI ウィンドウ・枠・文字** | **DQ4/5** | 現状の `#000018` 紺地＋青二重枠が既に DQ 寄り。勇なま自身も UI は DQ の作法を踏襲している |

### 勇なまの絵を、作品名に頼らず言語化したもの

**発注先が作品を知らなくても再現できるよう、必ずこの記述を渡すこと。**

- **地層は暖色**。土＝ベージュ〜茶、岩＝青みの灰、水＝明るい水色、溶岩＝オレンジ。
  黒地ではなく**明るい断面**が画面の大半を占める
- **各層はベタ塗り2〜3階調**。グラデーションを使わず、明部と暗部の2色＋境界の輪郭で立体を出す
- **すべての要素に濃い輪郭線（アウトライン）が入る**。これがコミカルさの要。線が無いと途端に安っぽくなる
- **モンスターは「単純な塊＋大きな目」**。体は丸か台形などの一塊、パーツを足さない。1体3〜4色
- **影を落とさない。完全にフラット**。落ち影・ぼかし・光沢は使わない
- 動きは**弾む・ぷるぷるする**（squash & stretch）。硬い平行移動をしない

### DQ4/5 のウィンドウを言語化したもの

- ほぼ純黒の地に、白1px の枠線。**外側と内側の二重**、角は1px 落とし
- 装飾・角丸・影・グラデーションは**一切無い**
- 文字は白のビットマップ。選択カーソルは `▶`
- **本作は紺地＋青枠という独自色で既にこれをやっている。壊さず維持する**

---

## 全発注に共通の制約（各プロンプトに必ず貼ること）

```
■ 触ってはいけないもの
- レイアウト（.screen-layout / #gameCenter のグリッド、grid-template-areas）
- ゲームロジック・乱数消費。黄金ハッシュ path: bf386fb2 / sim: 3ceb1848・rngCount 437 を
  1ビットも動かさないこと。動いたら実装が壊れている合図
- test/p5/harness.js（回帰テストの土台）

■ 設計上の不変条件
- 正典ファイルは index.html 一本。外部リソースゼロ・依存ゼロ
- 描画は2階調ベタ塗り。毎フレームのグラデーション生成と ctx.shadowBlur は禁止
- 色は必ず :root のデザイントークン var(--c-*) を参照。生の色コード直書きをしない
- 余白は 4px 刻み（--s-*）。7px や 13px のような目盛り外の値を持ち込まない

■ 検証（作業後に必ず走らせ、結果を報告すること）
  test/p5/verify.sh index.html            # 黄金ハッシュ照合
  node test/p5/harness.js index.html design
  node test/p5/harness.js index.html colosseum
```

---

# 発注1 — デザイン規約の確定【最初に出す・小】

> **これは絵を描く発注ではない。以降3本の憲法を作る作業。**

## プロンプト

```
Magika・Dorue（index.html 一本の単一HTMLゲーム）のデザイン規約を確定させたい。
絵は描かない。規約を文章として2箇所に書くだけの作業。

【背景】
UI に「AI が作った感」がある。原因を実測したところ3点に絞られた：
① UI アイコンに絵文字を使っている（83種類・354箇所）。OS ごとに絵が変わり、
   色・線幅・パースの制御を放棄している
② font-family: 'Courier New' に日本語グリフが無いため、英数字＝Courier New、
   日本語＝OS のゴシック、アイコン＝カラー絵文字と、3種の書体が同時に出ている
③ 星屑や多層 text-shadow など「それっぽさ」を記号で後から足している

【参照作品（世界観の基準）】
- 盤面（地層・モンスター・勇者・城）＝『勇者のくせになまいきだ。』（PSP, 2007）
- UI ウィンドウ・枠・文字＝ ドラゴンクエスト4/5（FC/SFC）
※ この2つは別方向なので、上記のとおり領域で切り分ける。混ぜない。

【やること】
A. index.html の :root 直上のコメント（現在 8-11行目「デザイントークン（P4-1）」）に、
   既存の「生の色コード直書きをしない」と同じ形式・同じ場所で、以下を追記する：

   - UI アイコンに絵文字を使わない。アイコンは 1px グリッドに乗せた矩形の集合として
     インライン SVG で描く（後述の発注3で置き換える）
   - 和文と欧文で同じ書体系列を使う。日本語グリフを持たないフォントを
     font-family の先頭に置かない
   - 影・ぼかし・グラデーション・光沢を「雰囲気を出すため」に足さない。
     立体は2階調のベタ塗りと1pxの輪郭線だけで表現する
   - 参照作品と領域の切り分け（上記）を明記する

B. magi_CLAUDE.md に「デザイン規約」の節を新設し、同じ内容＋
   上の参照作品の言語化（下記）を書く。作品名を知らない者でも再現できるようにする：

   [勇なまの絵の特徴]
   - 地層は暖色。土＝ベージュ〜茶、岩＝青みの灰、水＝明るい水色、溶岩＝オレンジ。
     黒地ではなく明るい断面が画面の大半を占める
   - 各層はベタ塗り2〜3階調。グラデーションを使わず、明部と暗部の2色＋境界の輪郭で立体を出す
   - すべての要素に濃い輪郭線が入る。これがコミカルさの要
   - モンスターは「単純な塊＋大きな目」。体は一塊、パーツを足さない。1体3〜4色
   - 影を落とさない。完全にフラット
   - 動きは弾む・ぷるぷるする（squash & stretch）

   [DQ4/5 のウィンドウの特徴]
   - ほぼ純黒の地に白1px の枠線。外側と内側の二重、角は1px 落とし
   - 装飾・角丸・影・グラデーションは一切無い
   - 本作は紺地＋青枠という独自色で既にこれをやっている。壊さず維持する

【触ってはいけないもの】
- CSS の値そのもの。この発注ではコメントとドキュメントだけを書く
- レイアウト、ゲームロジック

【完了条件】
- index.html の変更がコメント行のみであること（差分を見せて確認できる状態）
- test/p5/verify.sh index.html が通り、黄金ハッシュが動いていないこと
```

---

# 発注2 — フォントの統一【独立・小】

> **⚠ 発注前にユーザーの判断が1つ要る（下記）。**

## 判断待ちの論点：外部リソースゼロをどうするか

本作は「外部リソースゼロ」が設計上の不変条件。しかし**日本語グリフを持つピクセルフォントは
システムに存在しない**ので、次のどれかを選ぶ必要がある。

| 案 | 内容 | 代償 |
|---|---|---|
| **A. 埋め込み** | PixelMplus 等をサブセット化し base64 で index.html に直接埋め込む | ファイルサイズ +数十KB〜。単一HTML・外部ゼロは**維持できる** |
| **B. 外部配信** | Google Fonts の `DotGothic16`（日本語収録・ピクセル体）を読む | **外部リソースゼロを破る**。オフラインで崩れる |
| **C. 諦める** | ピクセルフォントを使わず、和文も出る等幅システムフォントに統一する | 3書体混在は解消するが、ドット感は出ない |

**推奨は A。** 単一HTMLという本作最大の資産を壊さずに済み、③の「記号で足す」を避けて
書体という土台から世界観を作れる。サブセット化（使用文字のみ抽出）すれば数十KB に収まる。

## プロンプト（案A を選んだ場合）

```
Magika・Dorue（index.html 一本の単一HTMLゲーム）のフォントを統一したい。

【現状の問題】
font-family: 'Courier New', Courier, monospace が全体に指定されているが、
Courier New に日本語グリフが無い。そのため実際の画面には
  英数字 → Courier New（タイプライター体）
  日本語 → OS のデフォルトゴシック（Hiragino / Yu Gothic）
  アイコン → カラー絵文字
の3種類が同時に出ており、これが「まとまりの無さ」の物理的な原因になっている。
Courier New は「レトロ」を狙うとき最も安易な選択で、
本作が目指すドット絵の世界観（＝ビットマップフォント）とは無関係でもある。

【やること】
1. 日本語グリフを持つピクセルフォント（PixelMplus12 または同等のもの。
   ライセンスが再配布可能なものに限る）を選ぶ。選定理由とライセンスを報告すること
2. index.html 内で実際に使われている文字だけを抽出してサブセット化する
   （日本語フルセットは1MB級になるため必須）
3. base64 で index.html 内の @font-face に直接埋め込む。
   ★ 外部リソースゼロ・単一HTMLという本作の不変条件は絶対に壊さないこと
4. font-family の指定を全箇所（現在9箇所）新フォントに差し替える。
   フォールバックには monospace を残す
5. フォントサイズがピクセルフォントの推奨サイズ（12px の整数倍など）から
   外れている箇所は、読めなくなるので合わせる。
   ただし ★レイアウトが崩れないこと★ を目視とテストで確認する

【触ってはいけないもの】
- レイアウト（.screen-layout / #gameCenter のグリッド）
- ゲームロジック・乱数消費

【完了条件】
- 画面上のすべての文字が単一の書体系列で表示される
- 追加サイズが +100KB 以内に収まっている（超える場合は報告して指示を仰ぐ）
- test/p5/verify.sh index.html が通り、黄金ハッシュが動いていないこと
- node test/p5/harness.js index.html design が通ること
```

---

# 発注3 — アイコン83個の置き換え【中〜大・2段に切る】

> **⚠ 一括で83個作らせないこと。** 方向性が違ったとき全損になる。
> **必ずパイロット10個 → ユーザー承認 → 残り**の順で回す。

## 3-a. パイロット（先に出す）

```
Magika・Dorue（index.html 一本の単一HTMLゲーム）の UI アイコンを、
絵文字からドット絵の SVG に置き換えたい。まずパイロットとして10個だけ作る。

【現状の問題】
UI アイコンに絵文字を 83種類・354箇所使っている（CSS 1 / HTML 96 / JS 240）。
絵文字は OS ごとに絵が変わるため、色・線幅・パースの制御を完全に放棄している。
Apple Color Emoji のつるっとした3D調のアイコンが、#000018 のダークな画面に
83種類バラバラに散っており、これが「AI が作った感」の最大の原因になっている。

【目指す絵柄】
ドット絵。参照は『勇者のくせになまいきだ。』（PSP, 2007）と DQ4/5。
作品を知らなくても再現できるよう、満たすべき条件を挙げる：
- 16×16 または 12×12 の 1px グリッドに完全に乗せる。グリッド外の座標を使わない
- 曲線・斜線・アンチエイリアスを使わない。矩形の集合だけで描く
- 1アイコン3〜4色まで。すべてに濃い輪郭線を入れる
- 影・ぼかし・グラデーション・光沢を使わない。完全にフラット
- 色は :root のデザイントークン var(--c-*) を参照する。
  単色でよいものは currentColor にして CSS 側から色を変えられるようにする

【技術方針】
- インライン SVG。<rect> の集合として 1px グリッドに乗せる
  （ベクターなので拡大しても崩れず、色を currentColor で差し替えられる）
- shape-rendering="crispEdges" を必ず付ける
- 同じアイコンが複数箇所に出るので、<symbol> + <use> で定義を1箇所にまとめる

【この発注でやること】
出現頻度の高い順に10個だけ作る。おそらく：
  💎 ダイヤ(28回) / ⚔ 攻撃(14) / 🚩 指令旗(13) / 🏳 門(9) / ⚗ 錬成(9)
  ★ 星(8) / 🛡 防御(7) / 📤 (7) / 🏦 銀行(6) / 🏆 実績(6)
※ 実際の出現頻度は自分で数えて確定させること。上記は参考値

1. <symbol> 定義を index.html の <body> 直後にまとめて置く
2. 該当10種の出現箇所すべてを <use> に差し替える
3. 縦位置・サイズが絵文字と揃うよう CSS クラス（.ico）を1つ用意する

【触ってはいけないもの】
- レイアウト、ゲームロジック、乱数消費
- 残り73種の絵文字（この発注では触らない）

【完了条件】
- 10種すべてが置き換わり、画面上で絵文字と混在した状態でも位置がずれていないこと
- test/p5/verify.sh index.html が通り、黄金ハッシュが動いていないこと
- node test/p5/harness.js index.html design が通ること（性能予算の砦）
- ★ 作った10個を、ユーザーが1個ずつ確認できる形で提示すること
  （dev/screens.html に一覧を出すか、16倍拡大した確認用ページを作る）
```

## 3-b. 残り（パイロット承認後に出す）

```
（3-a と同じ「目指す絵柄」「技術方針」「触ってはいけないもの」を貼る）

【この発注でやること】
パイロットで確定した絵柄に揃えて、残りの絵文字をすべて置き換える。

★ ただし機械的に73個作らないこと。先に棚卸しをして、次の3つに仕分ける：

  (1) UI アイコンとして必要なもの → SVG 化する
  (2) 意味が重複しているもの → 1つに統合する
      （UI アイコンは通常20〜30種類あれば足りる。83種類は多すぎる。
        統合案を提示して承認を得てから進めること）
  (3) 画面上のメッセージ文（電光掲示板 #message やログ）の中に
      文章の一部として出ている絵文字 → これはアイコンではないので別扱い。
      落とすか SVG にするかを個別に判断し、一覧にして提示する

JS 内に240箇所あるが、その多くは (3) のメッセージ文字列の可能性が高い。
文字列連結の中に SVG を差し込むと壊れやすいので、
DOM 生成箇所と innerText 代入箇所を区別して扱うこと。

【完了条件】
- 画面上から絵文字が消え、すべてドット絵 SVG か文字に置き換わっていること
- 統合によってアイコン種類数がいくつになったかを報告すること
- test/p5/verify.sh / design / colosseum の3つが通ること
```

---

# 発注4 — 盤面の絵柄を勇なま方向に【大・最難関】

> **最も難しく、最も効く。** 発注3までが終わってから出すこと。
> canvas 描画なので性能予算（`shadowBlur` 0件・毎フレームのグラデ生成0件）が直接効いてくる。

## プロンプト

```
Magika・Dorue（index.html 一本の単一HTMLゲーム）の盤面の絵柄を作り直したい。
地中の断面を掘り、モンスターを育て、勇者を迎え撃つゲーム。

【現状】
- 地形は fillRect のベタ塗り（index.html:6630 付近。土 #795548 / 水 #87CEEB など）
- エンティティは drawEntity（index.html:6766）が曲線を含むベクター描画で描いている
- ハイブリッド（異種交配）は bodyOf(mtype) の体に drawCoat で素材を4段重ねる構造。
  4形 × 4素材 = 16通りある

【目指す絵柄】
『勇者のくせになまいきだ。』（PSP, 2007）。本作と同じ「地中の断面を掘る」ゲーム。
作品を知らなくても再現できるよう、満たすべき条件を挙げる：

- 地層は暖色。土＝ベージュ〜茶、岩＝青みの灰、水＝明るい水色、溶岩＝オレンジ。
  黒地ではなく明るい断面が画面の大半を占める
- 各層はベタ塗り2〜3階調。グラデーションを使わず、明部と暗部の2色＋
  境界の1px 輪郭で立体を出す
- すべての要素に濃い輪郭線が入る。これがコミカルさの要。線が無いと安っぽくなる
- モンスターは「単純な塊＋大きな目」。体は丸か台形などの一塊にし、パーツを足さない。
  1体3〜4色
- 影を落とさない。完全にフラット
- 動きは弾む・ぷるぷるする（squash & stretch）

【★ 最重要の制約：性能予算】
- ctx.shadowBlur は1箇所も使わない
- 毎フレームのグラデーション生成をしない（createLinearGradient を描画ループ内で呼ばない）
- 現行実装はこの予算を全16通りで実測クリアしている。維持すること
- node test/p5/harness.js index.html design がこれを検査している

【★ 構造上の制約：ハイブリッド】
体(body)の描画を流用し、素材(coat)を上から重ねる構造を壊さないこと。
壊すと16通りの組み合わせが破綻する。特に：
- traceFormSilhouette（index.html:6706）は体の輪郭のパスだけを作る小関数。
  HPバーが素材色に染まらないよう、体の輪郭だけを対象にしている。
  この分離を維持すること
- ちびは成体の 0.55 倍スケールで描かれる。ドット絵にすると
  0.55 倍で潰れる可能性があるので、ちび用の描き分けが要るか検討して報告すること

【段取り（一括でやらないこと）】
1. まず地形だけ（土・岩・水・溶岩・宝石鉱脈）を作り直し、提示する
2. 承認後、モンスター1種（ゴブリン）だけを作り、成体・ちび・4素材の
   5通りを提示する
3. 承認後、残り3形（ゴーレム・レイス・もう1種）に展開する
4. 最後に勇者・城・門

【触ってはいけないもの】
- ゲームロジック・乱数消費。黄金ハッシュ path: bf386fb2 / sim: 3ceb1848・
  rngCount 437 を1ビットも動かさないこと
- TILE_SIZE などの盤面の寸法（レイアウトが連動している）
- 経路探索の深度非依存性

【完了条件】
- 各段階で、16倍拡大した確認用の画像かページを提示すること。
  30px タイルで動いている中で体と素材が判別できるかは、静止画では分からないので
  実際に動かした状態でも確認できるようにすること
- test/p5/verify.sh / design / colosseum / hybrid の4つが通ること
```

---

## 発注の順番と、なぜ4本なのか

```
発注1（規約）── 必須の先行。以降3本の憲法になる
   ├─ 発注2（フォント）── 独立して走らせられる。CSS のみ・低リスク
   ├─ 発注3（アイコン）── 3-a パイロット10個 →【ユーザー承認】→ 3-b 残り
   └─ 発注4（盤面）───── 3 の完了後。canvas・性能予算・最難関
```

**一括にしない理由:**

1. **絵柄の方向性は最初の10個で確認したい。** 83個作ってから「違う」となると全損する
2. **検証手段が違う。** 2 と 3 は CSS/DOM（`design` エピローグ）、4 は canvas の
   性能予算と `hybrid` の16通り。混ぜると、どのテストが何を守っているか分からなくなる
3. **リスクの階層が違う。** 1 はコメントのみ、2 は CSS のみ、3 は DOM 生成箇所に触る、
   4 は描画コードそのもの。まとめると、失敗したとき切り戻す単位が作れない
4. **1 を先にやらないと、2〜4 で同じ説明を毎回書くことになる。**
   規約に書いてあれば「規約に従え」で済む

## モデルの選び方

- **発注1・2** — 判断より作業量。Sonnet 5 で十分
- **発注3** — 絵柄の一貫性を83個にわたって保つ仕事。パイロット（3-a）は
  最も高性能なモデルで方向を決め、残り（3-b）は確定した絵柄に揃えるだけなので
  Sonnet でもよい
- **発注4** — 性能予算・16通りの組み合わせ・黄金ハッシュを同時に守りながら
  絵を作る。**ここだけは最も高性能なモデルを使う価値がある**

**注意:** Claude Fable 5.1 は「最も要求の厳しい推論と長期的なエージェント作業向け」であって、
**UI/フロントエンド特化のモデルではない**（$10/$50 per MTok・Opus 5 の2倍）。
今回の「AI 感」の原因はモデルの能力不足ではなく、**禁止事項が書かれていなかったこと**。
発注1をやらずにモデルだけ上げても、絵文字はまた出てくる。
