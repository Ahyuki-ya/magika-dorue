# セッション引き継ぎ（2026-07-24〜25）

Magika・Dorue（単一HTML・依存なし・git化済み）の開発セッション引き継ぎ。
正典ファイルは [index.html](index.html) 一本。直接編集 → commit → `git push origin main` →
GitHub Pages 自動公開（https://ahyuki-ya.github.io/magika-dorue/ ）。詳細仕様は
[magi_CLAUDE.md](magi_CLAUDE.md)（「その8〜その11」が今回分）。

起点コミット `89ca12a`（前セッション末）→ 今回 11 コミット（`15761fc`〜`4bc3883`）。すべて push 済み。

---

## 今回やったこと（時系列）

### 1. Phase 5：経路探索・時間系の保守的最適化（指示書 `OPUS_実装指示_大型改修.md`）
**「挙動を1ビットも変えない（保存則1〜3）」制約付きの純最適化。** 等価性ハーネスで機械照合済み。
- **P5-1 世代スタンプ法**：BFSの毎クエリ `new Int32Array(ROWS*COLS)` を共有スクラッチ＋世代スタンプに置換（GC圧ゼロ）。
- **P5-2 城フローフィールド**：`getNextStepTowardsCastle` を距離場greedy化（地形変更時のみ再計算）。`__p5_bumpTerrain()` を dig/勇者の家/expandMap/generateMap に配線。
- **P5-4 追跡BFSホライズン化**：`getNextStepTowards` を半径 `H=3×range` に制限＋全域フォールバック（強保存）。
- **P5-5 フレームゲート繰越** / **P5-6 gameTime一本化**（ポーズ中の壁時計進行バグを解消）。
- **P5-3（Union-Find）はユーザー判断で見送り**（「到達不能ターゲットはいない」）。
- 実測：深度250で城直行 約5900倍・追跡 約23倍高速、**深度非依存**を達成。

### 2. レベル上限（cap）方式へ変更 ＋ ランダム強化廃止（`cdd6f46`）
- **ダイヤ=上限解放 / ゴールド=実レベル**。上限 `monsterCap`（初期5・最大100・恒久 `magika_monstercaps`）を 1💎で+1（`raiseCapDiamond`）。ゲーム内ゴールドの `levelUp` は上限まで。旧セーブ救済 `max(5, 既存Lv)`。
- **ランダム強化(🎲)を廃止**（強すぎ）。`randBonus` は装備アフィックスの器として残置。

### 3. 開発者コマンド（`6354d97`）
- タイトル画面で **Shift+Cmd(またはCtrl)+Enter** → 銀行残高(`magika_carryover`)を prompt で自由設定（マイナス可）。

### 4. 宝石鉱脈・レア度・アフィックスの再設計
- **宝石鉱脈率**（`acddd5a`→`4bc3883`）：`pGem = 0.005×min(1,depth/1000)`（深度1000で標準0.5%）に **×L**（standard=×1, hard=L倍）。
- **レア度分布**（`61aa68b`→`4bc3883`）：`rollRarityIndex(depth, L)`。実効深度 `dEff=depth×L` で分布を計算。アンカー 深度0/500/1000＝並[80,60,40] 良[15,25,35] 稀[4,10,16] 極[1,4,7]。**伝説は実深度500以上で解禁**（高Lでも浅部で出さない）・上限2%。
- **アフィックス拡張**（`c9f8377`→`681ff85`）：`atk/agi/hp` を表示名「基礎攻撃/敏捷性/体力」に（キー据置）。新規 `mulAtk/mulAgi/mulHp`（倍率＝0〜1%×レア度mult の小数）、`range`（射程＝攻撃距離、**シャドウレイス限定**で遠隔攻撃）、`sight`（索敵＝探知距離、全モンスター）。
- **`effStat(mt)` 導入**（C-5）：召喚/繁殖/成長/レベル反映の全経路を統一。攻撃判定 `minDist <= (atkRange||1)` で遠隔攻撃対応。

### 5. 闇水晶を通常鉱石化（`de1e731`）
- 闇水晶(7)を先行ロール＋レバレッジ変調から外し、銅/石/苔と同じ基本分布ロールに。`levTreasureFactor` と LEV_TREASURE定数を削除。

### 6. Shift+クリックで城設置（`c227464`）
- canvas mousedown で `e.shiftKey` なら `placeCastle`（掘削しない）。

---

## 現在のゲーム状態（要点）

- **モンスター成長**：`mstatsAt(mt,lv)` = 線形（base+slope）。上限は cap（ダイヤ解放）。実効値は `effStat(mt)`（基礎加算×倍率、range=探知+索敵、atkRange=レイスのみ1+射程）。
- **宝具**：宝石鉱脈(8)を掘ると100%ドロップ。レア度は `rollRarityIndex(depth, L)`。合成＝同レア10+核1→次レア（系統継承）。神は合成専用。装備3枠、`equippedAffixTotals` で集計。
- **レバレッジ（hardのみ, L=2/3/5/10）**が効くもの：開始資本S×L、討伐G×L、ダイヤ率×L、精算ΔB=gold−S×L、**宝石鉱脈率×L**、**レア度分布（実効深度×L）**、勇者の富スケール（hard・現在gold比例）。
- **鉱石発生**：銅/苔=`0.33×min(1,depth/200)`、闇水晶=深度100から線形12%（レイス解禁時）、いずれも基本分布ロール。宝石鉱脈のみ先行ロール。

---

## テストハーネス（重要）

`test/p5/`（`test/p5/verify.sh` で実行）。**「挙動を変えない改修では必ず緑にする」回帰ガード。**
- `verify.sh [対象html] [基準ref]`：経路探索オラクル(19926)・全軌道sim(1000tick)・div balance を Phase5直前(89ca12a)と照合。黄金ハッシュ path=`a72aec6f` / sim=`f554277b`。
- 個別計測エピローグ：`epilogue_{gem,rarity,affix,cap,dark,levtz,perf,pause}.js`（`node harness.js <html> <mode>`）。バランス数値の実測に使える。

---

## 未着手・保留・次の候補

- **宝具リスト.md の拡充**：ユーザーが [宝具リスト.md](宝具リスト.md) にアイコン/名前を追記 → `TREASURE_NAMES` に反映する運用（まだ空欄多数）。効果表は最新（基礎/倍率/射程/索敵）に更新済み。
- **バランス調整の可能性**（数値は全て定数化・調整容易）：
  - 倍率アフィックスの base範囲 `[0,1]`（`AFFIX_TYPES`）、射程 `[1,1]`。
  - 宝石鉱脈の係数 `0.005`、レア度アンカー、レバレッジの掛け方（現状 gem=×L / rarity=実効深度×L）。
  - レベル上限初期値5・ダイヤ1個=上限+1、ゴールドのLvアップ費用 `goldCostFor`。
- **ロードマップ（`OPUS_実装指示_大型改修.md`）の残り**：Phase 3 取引コード（道A・焼却付き譲渡）、Phase 4 デザイン強化。※Phase 1(レバレッジ)/2(宝具)/5(最適化)は実装済み（Phase1は預入モデルAで指示書版と別設計）。
- **タッチ環境の城設置ショートカット**：現状 Shift+クリックはマウスのみ。必要なら長押し等で対応可。

## メモリ（`.claude/.../memory/`）
- `project-overview.md` / `leverage-design.md`（その11まで反映）/ `phase5-pathfinding.md` / `MEMORY.md`（索引）。
