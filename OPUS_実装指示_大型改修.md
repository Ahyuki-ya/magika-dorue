# Magika・Dorue 大型改修 実装指示書（Opus 引き渡し用・確定版）

> 基準ファイル: **`magi_claud6.html`**（3,179行）。作業はこれを `magi_claud7.html` にコピーしてから行うこと（スナップショット規約）。
> 本書は `FABLE_要件定義_大型改修.md` の検討事項をすべて**決定**に落とし、実装タスクを細分化したもの。
> 数値バランスはすべて「初期値」であり、各Phaseの `CONFIG` 定数に集約してチューニング可能にすること。

---

## 0. 決定事項サマリー（FABLE要件書の検討事項への回答）

| 論点 | 決定 | 根拠 |
|---|---|---|
| レバレッジのリスク乗せ先 | **A（被ダメ×L）主軸 ＋ C改（賭けノルマ制）** | 本作はエンドレス型で城崩壊が唯一のラン終了。「敗北時没収」は毎ラン発動する単なる税になるため、Cは「ノルマ未達時のみ没収」に再設計 |
| 倍率の刻み | **1x / 2x / 3x / 5x の離散4択** | UI簡潔・期待値表示しやすい |
| 倍率の変更タイミング | **ラン開始時固定**（ハード選択→倍率選択の2段階） | ラン中変更は日和見を許し賭けが成立しない |
| 対象モード | **ハードのみ**（スタンダードは常にL=1相当・UI非表示） | ユーザー指定「hardモードでは勝敗にレバレッジ」 |
| 宝物affixバランス | 8種プール・レア度別個数 1/1/2/2/3・値域テーブル固定 | §2参照 |
| 装備スロット | **3枠・レア度制限なし** | シンプル優先 |
| クラフト（分解合成） | **スコープ外** | スコープ膨張防止 |
| 取引の道 | **道A（コード交換）のみ実装。道Bはスコープ外** | 要件書の推奨ロードマップ第1段階 |
| 取引モデル | **焼却付き譲渡**（エクスポート元は使用不可化） | 贈与(コピー)より経済的整合、価格付き売買は道B送り |
| 取引通貨 | **なし**（無償譲渡のみ） | 通貨精算はサーバー権威なしでは成立しない |
| GitHub Pages | **単独公開を維持**（単一HTML・外部依存なし堅持） | 現行思想 |
| デザイン方針 | **レトロRPG様式を深掘り**（トークン化・質感・動き・可読性）。全面リデザインはしない | §Phase 4参照。外部アセット禁止と両立 |
| 経路探索の最適化 | **強保存（軌道一致）の範囲のみ実施**: 距離場化・Union-Find・世代スタンプ・ホライズン＋フォールバック | §Phase 5参照。距離場等価定理と単調性が根拠 |
| ロジック時刻 | **ゲーム内時計 `gameTime` へ一本化**（壁時計とフレーム時計の混在を解消） | ポーズ絡みの時間的不整合の修正。§Phase 5 P5-6 |
| 勇者の部屋（鋸歯状リセット） | **保留**（旧部屋の閉鎖可否・区間幅Wが未決） | ユーザー決定後に別紙で追加。§Phase 5 P5-B |

実装順序: **P4-1（デザイントークン）→ Phase 5（基盤最適化）→ Phase 1 → 2 → 3 → Phase 4残り**。各Phase完了ごとに動作確認。Phase 5 を先行させる理由: 挙動を変えない純最適化なので等価性検証（P5-7）が最も効く位置であり、後続Phaseで挙動が変わった後では「一致」の照合基準が失われるため。Phase間の依存は一方向（後のPhaseが前を参照）。デザイントークン（P4-1）だけは以降の全UI実装が参照するため最初に行う。

---

## 共通実装条件（全Phaseに適用・違反はレビューで差し戻し）

- **C-1. 単一HTML堅持**: 外部ファイル・CDN・ビルドツール禁止。全コードを `magi_claud7.html` 内に記述。
- **C-2. スナップショット規約**: `magi_claud6.html` を上書きしない。作業は `magi_claud7.html`。
- **C-3. 画面遷移は `showScreenOnly()` 経由**（1187行付近）。新画面は `ALL_SCREENS` 配列に必ず追加。直接 `style.display` を触る画面切替を書かない。
- **C-4. `oreProbAt(y)` は不変更**。マップ生成の確率追加は `extraOreAt(y)`（1438行付近）と同じ「先行ロール上乗せ」パターンで行う。
- **C-5. モンスターの実効ステータスは一元化**: Phase 2 で `effStat(mt)` ヘルパーを導入し（P2-6）、**掘削召喚・levelUp・levelDown・randomUpgrade・繁殖(子/成長)の全経路**がこれを使うこと。過去に randBonus の反映漏れバグが3経路で発生している（CLAUDE.md 2026-07-02 その3参照）。
- **C-6. localStorage の読み込みは必ず `Object.assign(defaults, JSON.parse(...))` + try-catch**。旧セーブとの後方互換を壊さない。
- **C-7. SEは `playSE(name)` に追加**。AudioContext を直接触らない。ミュート判定・try-catch は playSE 内で完結している。
- **C-8. 描画で `ctx.shadowBlur` 禁止**（パフォーマンス既知問題）。発光表現は半透明レイヤー重ねで。
- **C-9. ゲーム内UIは claud6 のスライドドロワー様式に従う**: 新タブは `.ig-tab` パターン（707行付近 `#inGamePanel`）。ドロワーはプレイを止めない（ポーズしない）仕様が現行。宝物庫タブも同様にライブ表示。
- **C-10. プレイヤーの自由テキスト入力を作らない**（取引の不正コンテンツ対策）。名称等はすべて固定テーブル参照。
- **C-11. 各Phaseの定数は `LEVERAGE_CONFIG` / `TREASURE_CONFIG` / `TRADE_CONFIG` オブジェクトに集約**し、コメントで「バランス調整点」と明示。
- **C-12. 検証**: 各Phase完了時に (a) `node --check` で構文、(b) DOMスタブでのトップレベル実行、(c) 本書の受け入れ条件をテスト。テストハーネスは末尾§5のものを流用。

---

## Phase 1: ハードモード・レバレッジ

### 仕様

**選択フロー**: モード選択画面（640行 `#modeSelectScreen`）でハードを押すと、直接開始せず**倍率選択パネル**を表示（同画面内に展開でよい）。1x/2x/3x/5x の4ボタン＋各ボタンに効果サマリー表示。選択で `enterGameScreen('hard', L)`。スタンダードは従来通り即開始（L=1固定）。

**効果（L = leverage）**:
| 項目 | 式 | 適用箇所アンカー |
|---|---|---|
| 勇者撃破ゴールド | `reward * L` | 2518行付近 `baseReward` 計算後 |
| ダイヤドロップ率 | 通常勇者: `min(base * L, 5 * L)` %／怒り勇者: 既存 `min(heroGold, 100)` のまま | 2536行付近 |
| 宝物ドロップ率 | `* L`（Phase 2 実装後に接続） | P2-4 |
| 城の被ダメージ | `castleHp -= entity.atk * L` | 2485行付近 |
| 賭けノルマ | L≥2 のとき、ラン終了時 `defeatedHeroesCount >= QUOTA(L)` で賭け成功 | triggerGameOver（1867行） |
| 賭け成功報酬 | ボーナスダイヤ `+L` 個（diamond に加算後、引継ぎ保存） | triggerGameOver |
| 賭け失敗没収 | `没収額 = floor((prevCarry + gold) * FORFEIT_RATE(L))` を引継ぎゴールドから減額 | triggerGameOver の `newCarry` 計算 |

**初期定数**（`LEVERAGE_CONFIG` に集約）:
```javascript
const LEVERAGE_CONFIG = {
    options: [1, 2, 3, 5],
    quota:   L => 10 * L,               // 賭け成立に必要な撃破数（L=1は常に成立扱い）
    forfeitRate: L => (L - 1) * 0.10,   // 未達時の引継ぎ没収率: 2x=10%, 3x=20%, 5x=40%
    bonusDiamond: L => L                 // 達成ボーナス💎
};
```

### タスク分割

- **P1-1** 変数 `let leverage = 1;` を追加。`enterGameScreen(mode, lev = 1)` にシグネチャ拡張し、`leverage = (mode === 'hard') ? lev : 1;` を初期化ブロックに追加。
- **P1-2** モード選択画面に倍率選択パネルを追加。ハードボタンの onclick を `showLeveragePicker()` に変更。パネルには各倍率の「報酬×L／被ダメ×L／ノルマ撃破数／失敗時没収%」を表示。既存のレトロRPGテーマ（青枠 `#4466cc`、`border-radius:2px`）に合わせる。
- **P1-3** 報酬倍率適用: 2518行付近 `const reward = (baseReward + rageBonus) * leverage;` に変更。フローティングテキストは L≥2 のとき `+${reward}G (x${leverage})` 表記。
- **P1-4** ダイヤドロップ率: ハード時 `diamondChancePct` 計算に倍率適用（上表の式）。スタンダードは不変更。
- **P1-5** 被ダメ倍率: 2485行付近 `castleHp -= entity.atk * leverage;`。
- **P1-6** ゲーム中の常時表示: `#heroStatsUI` 付近に L≥2 のとき「⚡x3｜ノルマ 12/30」のような進捗チップを表示（毎フレームDOM更新ではなく撃破時のみ更新）。
- **P1-7** triggerGameOver の精算: (a) 賭け判定 → ボーナス💎 or 没収を `newCarry` へ反映、(b) リザルト画面に「賭け倍率／ノルマ達成・失敗／没収額 or ボーナス💎」の行を追加（リザルトは `adjustResultScale()` が自動縮小するので行追加可）、(c) 没収時SE（`playSE('error')` 流用可）と達成時SE（`playSE('bossKill')` 流用可）。
- **P1-8** records 拡張（任意・推奨）: `magika_records` に `bestLeverageWin`（達成した最高倍率）を追加し、実績「大博打」（5xノルマ達成）を `ACHIEVEMENTS`（1133行）に追加。

### 受け入れ条件
1. スタンダードでは倍率UIが一切見えず、挙動が現行と完全一致（L=1）。
2. ハード3xで勇者撃破報酬が正確に3倍、城被ダメが3倍になる。
3. 撃破数がノルマ未達でゲームオーバー → 引継ぎゴールドが没収率どおり減額され、リザルトに明示される。
4. ノルマ達成 → 没収ゼロ＋💎+L が引継ぎダイヤに反映される。
5. 旧セーブデータで起動してもエラーが出ない。

---

## Phase 2: レア度付き宝物（Treasure）システム

### 仕様

**レアリティ判定（確定アルゴリズム）**:
```javascript
// TREASURE_CONFIG 内
// bonus = min(30, depth * 0.08 + heroLevel * 0.3 + (leverage - 1) * 4)
// r = Math.random() * 100 - bonus
// r < 1 → legendary / r < 5 → epic / r < 15 → rare / r < 40 → uncommon / それ以外 → common
```

**ドロップ契機**（すべて `rollTreasure(depth, leverage, heroLv)` を呼ぶ）:
| 契機 | 確率 | アンカー |
|---|---|---|
| 宝石鉱脈(タイル8)掘削 | `10% * leverage` | dig() 2128行付近の tile===8 分岐 |
| 怒り勇者（ボス）撃破 | 100% | 2526行付近 isEnraged 分岐 |
| 通常勇者撃破 | `1.5% * leverage` | 同撃破処理 |

**affixプール**（値はレア度インデックス t=0..4 でスケール）:
| stat | 効果 | 値域（t=0→4） | 出現制限 |
|---|---|---|---|
| `startGold` | 開始ゴールド加算 | +5/+10/+20/+35/+60 | なし |
| `mAtk`(種別付き) | 指定種の攻撃 | +1/+1/+2/+2/+3 | なし |
| `mHp`(種別付き) | 指定種の耐久 | +1/+1/+2/+3/+4 | なし |
| `mAgi`(種別付き) | 指定種の敏捷 | +1/+1/+1/+2/+2 | なし |
| `rewardPct` | 撃破報酬% | +5/+8/+12/+18/+25 | なし |
| `diaPct` | ダイヤドロップ率(加算pt) | +0.5/+0.8/+1.2/+1.8/+2.5 | なし |
| `breedCap` | 繁殖上限 | +1 固定 | rare以上のみ |
| `growMs` | ちび成長時間短縮 | -2000ms 固定（合計下限8000ms） | epic以上のみ |

affix個数: common 1 / uncommon 1 / rare 2 / epic 2 / legendary 3（同一statの重複は再抽選、種別付きstatは種別が違えば別扱い）。

**名称は固定テーブル**（C-10）。`TREASURE_NAMES[rarity]` = レア度ごとに8個程度の {name, icon} 配列を用意（例: common「錆びた剣🗡」「欠けた石🪨」… legendary「魔王の宝冠👑」「深淵の心臓🫀」…）。自由入力UIは作らない。

**データ構造**（要件書どおり）: `{ id, name, rarity, icon, affixes[], bornDepth, bornRun, createdAt, traded? }`。
`id` は `'tz_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2, 10)`（crypto不要、単一HTMLで完結）。

**保管・装備**:
- `magika_inventory`: 宝物配列（上限 **200**。超過時は取得不可＋警告表示）
- `magika_equipped`: 装備中 id 配列（最大3）
- `traded: true` の宝物は装備不可・再エクスポート不可（Phase 3 連携）

**UI**:
- メインメニューに「🎒 宝物庫」ボタン追加 → 新画面 `treasuryScreen`（`ALL_SCREENS` へ追加、`showScreenOnly` 使用）
- ゲーム内ドロワー（707行 `#inGamePanel`）に3つ目のタブ「🎒 宝物」を追加。claud6 の `renderShopInto` 方式に倣い `renderTreasuryInto(listEl, live)` で全画面/ドロワー共用
- 一覧はレア度降順ソート、各行: アイコン／名前／レア色枠／affix要約／装備トグルボタン
- ドロップ時演出: レア度色のパーティクル（`spawnBurst` 流用）＋レア度別SE（rare以上は `playSE('gem')`、epic以上は新SE `treasure` を追加）＋フローティングテキスト「🎁 {name}!」

### タスク分割

- **P2-1** `TREASURE_CONFIG`（レア度重み・affixテーブル・名称テーブル・ドロップ率・上限200）を定義。
- **P2-2** `rollTreasure(depth, leverage, heroLv)` 実装（判定式は上記を厳守）。単体でテスト可能な純関数にする。
- **P2-3** `loadInventory()/saveInventory()/loadEquipped()/saveEquipped()`（C-6 準拠）。
- **P2-4** ドロップ契機3箇所へのフック（上表アンカー）。インベントリ満杯時はドロップせず「宝物庫が満杯!」表示。
- **P2-5** 装備効果の集計: `computeEquipBonus()` → `{ startGold, rewardPct, diaPct, breedCap, growMs, mAtk:{slime..}, mHp:{..}, mAgi:{..} }` を `enterGameScreen`（1799行）の初期化で `equipBonus` 変数へ格納。gold 初期化式・BABY_GROW_MS・BREED_MAX 参照箇所・reward 計算・diamondChancePct にそれぞれ加算。
- **P2-6** **`effStat(mt)` ヘルパー導入**: `{atk, agi, hp, range}` を `MONSTER_STATS[mt][idx] + randBonus[mt] + equipBonus.mXxx[mt]` で返す。既存5経路（dig召喚／levelUp／levelDown／randomUpgrade／繁殖の子生成・成長）を全てこれ経由に置換（C-5）。
- **P2-7** `treasuryScreen` 追加（HTML＋`ALL_SCREENS`＋メインメニューボタン）。
- **P2-8** ゲーム内ドロワーに宝物タブ追加（`switchInGameTab` の分岐拡張、`renderTreasuryInto` 共用関数）。
- **P2-9** 装備トグル処理（3枠制限、traded品は不可、装備変更は**次ラン反映**と明記表示。ラン中の即時反映はしない — ライブショップと違い戦闘バランスに直結するため）。
- **P2-10** ドロップ演出（パーティクル色テーブル・SE `treasure` 追加・フローティングテキスト）。
- **P2-11** リザルト画面に「このランで入手した宝物」一覧行を追加。
- **P2-12** 実績追加: 「初めての宝物」「伝説の発掘者（Legendary入手）」を `ACHIEVEMENTS` へ。records に `treasuresFound` 加算。

### 受け入れ条件
1. `rollTreasure` を深度0/L=1で1万回回すと common が概ね60%前後、legendary が1%前後（±0.5pt）。深度300/L=5 では上位レアの比率が明確に上がる。
2. 装備した affix が次ランの該当パラメータに正しく加算される（startGold・撃破報酬%・モンスターステータス各種を個別確認）。
3. `effStat` 導入後、levelUp/levelDown/randomUpgrade/掘削/繁殖の全経路でステータスが一致する（randBonus・equipBonus 込み）。
4. 満杯（200個）時にドロップがブロックされ、エラーではなく警告表示になる。
5. 旧セーブ（inventory キーなし）で起動してもエラーなし・空の宝物庫が表示される。

---

## Phase 3: 取引コード（道A・焼却付き譲渡）

### 仕様

**コード形式**: `MGKT1.<payload>.<checksum>`
- `payload` = 宝物オブジェクト（`traded` フラグ除去済み）を `JSON.stringify` → UTF-8 → base64url（`+/=` を `-_` に置換・パディング除去）
- `checksum` = `payload + SALT` の FNV-1a 32bit を16進8桁。`SALT` は `TRADE_CONFIG` 内の固定文字列
- **注記をコード内コメントに必ず書く**: このチェックサムは「コピペ事故・破損検出」用であり、改ざん防止ではない。道Aはチート耐性を目標にしない（要件書の警告どおり）

**エクスポート（出品）**:
- 宝物庫の各行に「📤 譲渡コード発行」ボタン（traded品・装備中品は不可）
- 発行すると: (a) コードをテキストエリア表示＋コピーボタン、(b) 元の宝物に `traded: true` を立てて保存（焼却）、(c) 装備リストから除外
- 確認ダイアログ必須: 「発行するとこの宝物は自分では使えなくなります」

**インポート（入手）**:
- 宝物庫画面に「📥 コードを受け取る」→ 貼り付け用テキストエリア（**これは自由入力だが、パース対象であり表示テキストにはしない**）
- 検証手順（すべて通過で受領、1つでも失敗なら理由表示して拒否）:
  1. 形式・バージョンプレフィクス `MGKT1` 一致
  2. チェックサム一致
  3. JSONパース成功・必須フィールド存在・型一致
  4. **ホワイトリスト検証**: `name`/`icon` が `TREASURE_NAMES` に実在、`rarity` が5種のいずれか、affix の stat がプールに実在し value が該当レア度の定義値域内（範囲外は拒否）
  5. `id` が自分の `magika_inventory` に存在しない かつ `magika_trade_seen` に存在しない（二重受領拒否）
  6. インベントリ200未満
- 受領時: `magika_trade_seen` に id を追記、インベントリへ追加、入手演出（P2-10流用）

**表示**: traded品は宝物庫でグレーアウト＋「取引済み」バッジ。削除ボタン（確認付き）で整理可能。

### タスク分割

- **P3-1** `TRADE_CONFIG`（SALT・バージョン文字列・上限）定義、`fnv1a(str)`・`b64urlEncode/Decode` ユーティリティ実装。
- **P3-2** `exportTreasure(id)` → コード文字列生成＋焼却処理＋装備解除。
- **P3-3** `importTreasure(code)` → 上記6段階検証＋受領。戻り値 `{ok, reason?, item?}` の純関数部分（検証）と副作用部分（保存）を分離し、検証部を単体テスト可能に。
- **P3-4** 宝物庫UIに発行・受領の導線追加（全画面/ドロワー両方）。
- **P3-5** `magika_trade_seen` の読み書き（C-6準拠、配列上限1000で古い順に間引き）。
- **P3-6** 確認ダイアログ・結果表示（成功/拒否理由）。`confirm()` は既存 `quitToTitle` で使用実績あり、流用可。
- **P3-7** SE: 発行 `playSE('buy')` 流用、受領 `playSE('treasure')` 流用。
- **P3-8** CLAUDE.md 追記用ノート作成（本Phaseの仕様・キー・検証手順）。

### 受け入れ条件
1. エクスポート→同一ブラウザでインポート → 「二重受領」で拒否される（焼却＋seen の両方が機能）。
2. コードの1文字改変 → チェックサム不一致で拒否。
3. affix value を値域外に書き換えたコード → ホワイトリスト検証で拒否。
4. 正常な相互交換（別ブラウザ/シークレットウィンドウで検証）で宝物が移動し、元側は「取引済み」表示になる。
5. 不正コードを大量に貼ってもクラッシュしない（try-catch で拒否理由表示）。

---

## Phase 4: デザイン性の向上

### 方針

**「レトロRPGウィンドウ」の世界観を深める方向で磨く**。フラットモダンへの作り替えは行わない。既存のアイデンティティ（深いネイビー `#000018`・RPGウィンドウ青 `#4466cc`・NESゴールド `#ffcc00`・Courier New・角なし `border-radius:2px`）は維持し、質感・動き・一貫性・可読性を上げる。

**デザイン固有の制約**:
- **D-1. 外部リソース禁止**（C-1の再掲・厳守）: Webフォント・CDN・画像ファイルを読み込まない。フォントは現行のシステム monospace スタック（Courier New系）のまま。装飾はCSS・canvas描画・絵文字で完結。
- **D-2. canvas の `ctx.shadowBlur` 禁止は継続**（C-8）。ただし **DOM要素の `box-shadow`・CSSアニメーションは可**（既存でも使用実績あり）。発光表現の使い分け: canvas内=半透明レイヤー重ね、DOM=box-shadow。
- **D-3. パフォーマンス予算**: モンスター80体・早送り時に体感低下を悪化させない。毎フレームの新規グラデーション生成・パターン生成は禁止。タイル質感は**起動時に1回だけオフスクリーンcanvasへプリレンダ**して `drawImage` する方式に限定。
- **D-4. 色だけに頼らない**: レア度・状態の識別は必ず「色＋形/記号」の二重符号にする（色覚多様性対応）。
- **D-5. モバイルの transform scale レイアウトを壊さない**: `#gameCenter` のスケーリング前提を変更しない。追加UIは既存メディアクエリの流儀に従う。

### 仕様

**デザイントークン（CSS変数）**: `:root` に集約し、Phase 1〜3 の新規CSSは必ず変数を参照する（既存CSSの置換は「低リスク箇所のみ任意」。全置換は回帰リスクに見合わないため必須にしない）。

```css
:root {
    /* 基調 */
    --c-bg: #000018;         --c-bg-game: #000010;   --c-panel: rgba(0,8,32,0.92);
    --c-frame: #4466cc;      --c-frame-dim: #2244aa; --c-frame-faint: #1a2a60;
    --c-text: #f0f0f0;       --c-text-sub: #8899cc;  --c-text-faint: #6688aa;
    --c-gold: #ffcc00;       --c-dia: #00ffee;       --c-danger: #ff4422;
    /* レア度（Phase 2 と共有） */
    --r-common: #95a5a6; --r-uncommon: #2ecc71; --r-rare: #3498db;
    --r-epic: #9b59b6;   --r-legendary: #ffcc00;
}
```

**レア度の視覚言語**（Phase 2 のUIが参照する統一ルール）:
| レア度 | 枠 | バッジ（形の二重符号） |
|---|---|---|
| common | 実線1px `--r-common` | `C` |
| uncommon | 実線1px `--r-uncommon` | `U` |
| rare | 実線2px `--r-rare` | `R` |
| epic | 二重枠 `--r-epic`（box-shadowで外周） | `E` |
| legendary | 二重枠＋CSSアニメで枠色が金⇔白にゆっくり脈動 | `L★` |

### タスク分割

- **P4-1** （**最初に実施**）`:root` デザイントークン定義。Phase 1〜3 の新規CSSはすべて変数参照で書く。受け入れ: 新規追加CSSに生の色コード直書きがない（レビューで grep 確認）。
- **P4-2** タイル質感のプリレンダ: 起動時に石/苔石/銅/闇水晶/宝石鉱脈の 30×30 タイルをオフスクリーンcanvasへ1回だけ描画（2〜3階調のピクセルノイズ＋既存の模様）。描画ループの `fillRect` 群を `drawImage` に置換。空/あくま部屋/城は現行のまま。
- **P4-3** 深度トーン: マップ描画時、行の背景色を深度に応じて僅かに暗色へ線形補間（地表＝現行色 → 深度300で `#0a0a14` 寄り）。行単位の色計算のみでコスト増ほぼゼロ。深く掘る没入感を出す。
- **P4-4** エンティティの2階調シェーディング: スライム/ゴブリン/ゴーレムに明部・暗部の2トーンを追加（円/矩形の重ね描きのみ、パス数は現行+2以内/体）。レイスは現行の重ねレイヤー表現を維持。
- **P4-5** 画面遷移フェード: `showScreenOnly` が表示画面に `.fade-in` クラスを付与（CSS `opacity 0→1` 150ms）。ロジック変更なし・クラス付与のみ。
- **P4-6** リザルト演出: 数値のカウントアップ（rAFで600ms、獲得ゴールド・撃破数・ダイヤ）。Phase 2 実装後は入手宝物をレア度枠付きで表示。`adjustResultScale()` との併用を確認。
- **P4-7** タイトル画面の空気感: CSSのみで背景に微細な星屑（`radial-gradient` の多重背景を `@keyframes` でゆっくり流す）＋タイトルロゴの多層 text-shadow を1層追加。JS追加なし。
- **P4-8** ボタン状態の統一: 全ボタン共通で hover / `:active`（`transform: scale(0.97)`）/ disabled / `:focus-visible`（`outline: 2px solid var(--c-frame)`）をトークンで定義。タッチでも押下感が出る。
- **P4-9** HUD可読性: canvas上のフローティングテキストに `strokeText`（1px 黒縁取り）を追加し背景色と干渉しても読めるように。ボスHPバー・次勇者予告のスタイルをトークン準拠に微調整。
- **P4-10** レア度視覚言語の実装（上表）: Phase 2/3 の宝物リスト・ドロップ演出・リザルト表示が共通のCSSクラス（`.rar-common` 〜 `.rar-legendary` ＋ `.rar-badge`）を使う。

### 受け入れ条件
1. 早送り＋モンスター約80体で、改修前と比べ体感のフレーム低下がない（タイルプリレンダが正しく1回きりであること）。
2. ブラウザの開発者ツールでネットワークタブを確認し、HTML以外の外部リクエストが一切ない。
3. レア度が**色を抜いた状態（グレースケール想定）でもバッジと枠で識別できる**。
4. モバイル幅（<640px）でスケーリング・FAB・ドロワーのレイアウトが崩れていない。
5. 画面遷移・ボタン押下・リザルトで、追加した演出がすべて動作し、演出中も入力を阻害しない。
6. Phase 1〜3 で追加された新規CSSがすべて `--c-*` / `--r-*` トークンを参照している。

---

## Phase 5: 経路探索・時間系の保守的最適化

### 方針と保存則（このPhaseの憲法）

目的は「深度が増えても計算量・GC圧が発散しない系」への移行を、**挙動を変えずに**行うこと。以下の保存則を定義し、P5-1〜P5-6 はすべて**強保存**（保存則1を満たす）でなければならない。

- **保存則1（強・軌道一致）**: 同一の乱数列に対し、最適化前後で全エンティティの状態軌道（位置・hp・cd・行動タイミング）が完全一致する。
- **保存則2（乱数消費不変）**: 最適化が `Math.random()` の呼び出し回数・順序を一切変えない。経路探索は乱数を消費しない（乱数はランダムウォーク・各種抽選のみ）ため、探索内部の変更はこれを自然に満たすが、**探索の「結果」が変わると以降の分岐で消費がずれる**ので保存則1とセットで守る。
- **保存則3（レート厳密性）**: `cd -= T` のキャリー方式（平均行動レート = 正確に r/T 行動/フレーム、r = 4·log₂(agi+1)）を維持する。リセット方式（`cd = 0`）への変更は禁止。

**数学的根拠（実装者が理解しておくべき2つの定理）**:

1. **距離場等価定理**: 前向きBFS（方向順 `D = [右,左,下,上]`、parent traceback）が返す経路は「D-辞書式最小の最短路」である（BFSの各レベルの訪問順が前レベルの訪問順とDから誘導される安定順序であることから帰納的に従う）。一方、目標からの逆向きBFS距離場 d(v) に対し「近傍をD順に走査し d が1減る最初の方向を選ぶ」局所貪欲は、辞書式順序の接頭辞貪欲性により同じD-辞書式最小最短路を構成する。**ゆえに両者の返す「次の一手」は厳密に一致する**。一致条件は (a) 局所走査の方向順がBFSの `dirs` 配列と同一、(b) 通行判定クラスが同一、の2点のみ。
2. **単調性**: 本作の通行可能性は時間について単調非減少（掘削は通行可へ変えるだけ・城移動も旧位置0/新位置5で共に通行可）。ゆえに動的連結性は「挿入のみのUnion-Find」で O(α) 管理でき、到達不能判定を全域走査なしで先出しできる。到達不能時にBFSを起動せず null を返すのは、現行の「全域走査して null」と同一結果＝強保存。

### タスク分割

- **P5-1 世代スタンプ法（割当ゼロ化）**: `getNextStepTowardsCastle` / `getNextStepTowards` の `new Int32Array(ROWS*COLS).fill(-1)` を廃止。モジュールスコープの共有スクラッチ（`parent` 用 Int32Array・`stamp` 用 Int32Array・`gen` カウンタ）に置換し、「訪問済み ⇔ `stamp[i] === gen`」で判定。`expandMap` で ROWS が増えた時のみ再確保。クエリごとの初期化を O(V)→O(1) に、確保由来のGC圧（深度300・100体で概算15MB/s）をゼロにする。純実装詳細＝自明に強保存。
- **P5-2 城距離場（フローフィールド）**: 城を単一源とする逆向きBFSで距離場を通行判定クラスごとに構築（クラスは「勇者用＝全域」「モンスター用＝`monsterOnly`、y < SKY_LAYERS 除外」の2枚）。`getNextStepTowardsCastle` と、城を目標とする `getNextStepTowards` 呼び出し（fertile集合・城直行モード）を「D順走査で d が1減る最初の近傍」に置換。**Dの順序は既存 `dirs` 配列と完全一致させること（等価定理の一致条件）**。無効化は地形変更イベント（`dig` 成功時・`placeCastle`・`expandMap`）で dirty フラグを立て、次の参照時に遅延再計算。計算量: O(M·V)/ティック → O(V)/地形変更。
- **P5-3 Union-Find 到達可能性**: 通行可セルの単調挿入に合わせて併合する Union-Find を2クラスぶん維持（勇者用/モンスター用。空域経由の連結は勇者にのみ有効なため別管理が必要）。経路探索の冒頭で「発点と目標が別成分なら即 null」。経路により隣接union（掘削時に新規セルと通行可近傍4方向を併合、初期マップ生成後に一括構築）。
- **P5-4 追跡BFSのホライズン化**: 敵追跡の `getNextStepTowards` に探索半径 H を導入（既定 `H = 3 * entity.range`、`PATHFIND_CONFIG` に集約）。L1菱形の面積評価により訪問セル数 ≤ 2H(H+1)+1（H=36で約2,700、深度非依存の定数）。**H内で未発見なら現行同様の全域BFSへフォールバックする（強保存の要）**。打ち切って追跡放棄する実装（保存則1・2を破る）は禁止。フォールバックはP5-3により「到達可能だが経路長>H」の稀ケースに限定される。
- **P5-5 フレームゲートのキャリー化**: `gameLoop` のゲートを `lastFrameTime = timestamp`（代入）から `lastFrameTime += interval`（繰り越し）に変更し、`timestamp - lastFrameTime > interval * 3` 等の場合は再同期クランプ。根拠: rAFはディスプレイ周期 p の格子上でしか発火せず、代入方式の実効周期は ⌈interval/p⌉·p に量子化される（144Hzでは p=6.94ms、早送り interval=16.67ms → 実効20.83ms=48fpsと2割遅い）。繰り越しで平均周期が正確に interval に一致する（保存則3と同じキャリー原理）。60Hz環境では挙動差なし。
- **P5-6 ゲーム内時計への一本化**: `gameTime`（非ポーズ時のみ Δt を積分するミリ秒カウンタ）を導入し、**ロジックが参照する時刻**をすべて置換: スタック検出（`lastMoveTime`・2秒閾値）、城直行（`castleRushUntil`・5秒）、ちび成長（`bornAt` と `BABY_GROW_MS` 比較・成長バー描画）。視覚演出のみの `Date.now()`（レイスの揺らめき等）は対象外。効果: ポーズ中に壁時計だけが進むことによる「再開直後の一斉城直行・即成体化」を解消。非ポーズ時は挙動同一＝強保存＋バグ修正。
- **P5-7 等価性検証（受け入れの中核）**: テストハーネスで `Math.random` を決定的LCG（例: `x = (1664525*x + 1013904223) >>> 0; return x / 2**32`）に差し替え、同一シードで「最適化前(claud6)」「最適化後」を各1,000ティック実行し、全エンティティの (x, y, hp, cd) 系列と乱数消費回数が**完全一致**することを機械照合する。地形変更を含むシナリオ（掘削スクリプト再生）でも一致を確認。
- **P5-B（保留・実装しない）勇者の部屋／鋸歯状リセット**: 生きたマップを窓幅 W に制限し V を定数化する構想。**ユーザーの設計判断が未確定のため本Phaseに含めない**。未決事項: (a) 旧勇者部屋を閉じるか（複数方向襲撃の戦術性とのトレードオフ）、(b) 区間幅 W（縦長上限とスマホ/PC両対応アスペクト比を決める）。決定後に別紙で指示を追加する。

### 受け入れ条件
1. P5-7 の等価性テストが完全一致でパスする（位置・hp・cd・乱数消費回数。1,000ティック×掘削あり/なしの2シナリオ）。
2. 深度300相当のマップ・モンスター80体・早送りで、経路探索の1ティック総コストが深度23時と同オーダーに収まる（プロファイラまたは手動計測で確認。距離場再計算は掘削時のみ発生していること）。
3. `new Int32Array` がゲームループ経路上で呼ばれない（`expandMap` 時の再確保のみ許容）。
4. ポーズ→30秒放置→再開で、勇者の城直行モードが発火せず、ちびの成長タイマーが進んでいない。
5. `PATHFIND_CONFIG`（H係数・クランプ係数）が定義され、コメントで調整点と明示されている。
6. 保存則1〜3がコード内コメントで明記され、距離場の方向順が `dirs` と単一定義を共有している（重複定義の禁止）。

---

## 4. スコープ外（実装しないこと・提案もPhase完了後に）

- 道B（バックエンド/BaaS導入、価格付き売買、入札、ランキング）
- 宝物のクラフト（分解・合成・強化）
- 宝物名の自由入力・カスタムアイコン
- リアルマネー要素・換金性のある仕組み
- Webフォント・画像アセットの導入、フラットモダンへの全面リデザイン、テーマ切替機能

## 5. テストハーネス（各Phase完了時に実行）

過去セッションで使用済みの Node + DOMスタブ方式を流用する:
1. `<script>` 部を抽出して `node --check`
2. DOMスタブ（getElementById が自動生成する makeEl、localStorage スタブ、AudioContext undefined）上でトップレベル実行
3. スクリプト末尾にテストコードを**連結**して eval（evalの `let` スコープ問題のため、外から参照しない）
4. Phase固有の検証: P5=決定的LCGによる軌道等価テスト（P5-7が詳細仕様）、P1=報酬/被ダメ/精算の数値検証、P2=rollTreasure 1万回分布・effStat 全経路一致、P3=エクスポート/インポートの往復・拒否系5パターン、P4=手動確認が主（受け入れ条件1〜5はブラウザ実機で、条件6はgrepで確認）

## 6. 完了時の納品物

- `magi_claud7.html`（全Phase実装済み・検証済み）
- `CLAUDE_md_追記_<日付>.md`（決定事項・新規localStorageキー・既知の調整ポイント）
- テスト実行ログ（受け入れ条件との対応が分かる形）
