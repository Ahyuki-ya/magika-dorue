# セッション引き継ぎ（2026-07-30）

Magika・Dorue（単一HTML・依存なし・git化済み）の開発セッション引き継ぎ。
正典ファイルは [index.html](index.html) 一本。直接編集 → commit → `git push origin main` →
GitHub Pages 自動公開（https://ahyuki-ya.github.io/magika-dorue/ ）。
詳細仕様は [magi_CLAUDE.md](magi_CLAUDE.md)（「その20〜21」が今回分）。

起点コミット `a767893`（前セッション末）→ **`3ba18f0`（1コミット）**。push 済み。

**今回の主題は「コロシアムモードの新設」。** 設計の全文は
[コロシアムモード_設計案.md](コロシアムモード_設計案.md)（実測値つき・次にバランスを触るときはここを見る）。

---

## 今回やったこと

### 1. コロシアムモード（standard / hard に続く第3のモード）

**芯**: 「有限の壁を、いつ兵に換えるか。掘り広げれば兵は増えるが、繋げた門からも勇者が来る」

| 要素 | 仕様 |
|---|---|
| 盤面 | **21×21固定・スクロールなし**。内側は全部壁（**361マス＝兵の在庫**）、穴は四方の門1マスずつだけ |
| 城 | **プレイヤーが置く**。門から掘り進んだ場所にのみ配置でき、置いた瞬間に第1波の準備が始まる |
| 侵攻 | **城に到達できる門からしか来ない**（`colOpenGates()`）。掘り広げて別の門に届くとそこも攻め口になる |
| wave | 体数 `1+floor((N-1)/3)` cap 8。勇者は**全員 Lv=wave**（富スケール無効＝記録が比較可能）。10の倍数はボス1体 |
| 指令 | **種別ごとの集合地点**（`colRally`）。未設定はランダムウォーク、`fertile` は繁殖のため城を優先 |
| 兵站 | 完全有限＋繁殖のみ。**繁殖は掘削とは別枠**（`COL_BREED_POP=12` / `COL_ARMY_CAP=100`） |
| 経済 | 討伐 `15+wave` G。**実レベルは Lv1 スタートで保存しない**。ゴールドは銀行に入らない |
| メタ報酬 | 💎は**最高記録の更新時のみ** `floor(best/5)` の差分（`magika_colosseum_best`） |

**ユーザーが決めた分岐**（迷ったらここに戻る）: 正方形アリーナ・四方侵攻／完全有限（繁殖のみ）／
到達wave記録で💎／複数体同時＋準備時間／**迷路づくりを残す（初期通路なし・城は自分で置く）**／**指令機能**。

### 2. 強化（Lv上げ・宝具）を場にいる個体へ即反映（全モード共通）
- `applyEquippedAffixes()` を `renderTreasuryAll()`（装備・解除・鍛錬・昇格・受領・削除の唯一の合流点）から呼ぶ
  → **ラン中に宝具を変えても即反映**される（従来は次のランまで反映されなかった）
- `refreshMonsterStats()` で**場にいる個体にも反映**。`applyMonsterLevel()` はこれを呼ぶだけの薄いラッパ
- 規約: **ちびは成体の半分／HPは割合を維持**（付け外しで回復させない）
- **`startGold` は開始時限定**（ラン中に反映すると付け外しで無限に稼げる）
- 併せて旧 `applyMonsterLevel` の不備2件を修正（ちびを成体扱いしていた／HPが `Math.min(hp,maxHp)` で
  割合維持でなかったため、Lvを上げると満タンの兵が相対的に欠けていた）

### 3. その他
- **構造的変更は `const COLS = 20` → `let COLS`（+`BASE_COLS`）の1つだけ**で済んだ。参照側は全て実行時に読む
- `isReachable` の `ny >= SKY_LAYERS` が北の門(y=0)を弾く不具合を修正（`skyLimit()` に置換。standard では同値）

---

## 実装中に3回、実測で前提が崩れた（同じ轍を踏まないため）

**このモードは「作って測る」を繰り返さないと成立しなかった。** 記録しておく。

### ① 繁殖のボトルネックが存在しなかった
「城の隣接8マスが繁殖の律速になる」と見込んでいたが、**エンティティは同じマスに重なれる**ので律速にならない。
実測で同時240〜431体まで膨れ、Lv10でも40wave完走・城無傷になった。

### ② 枠を共通にすると「掘る楽しさ」が死ぬ（最重要）
①の対策として「掘削と繁殖で共通の軍上限」を入れたところ、**繁殖は毎フレーム自動判定されるぶん、
戦闘で空いた枠を先に埋めてしまう**。実測で w1〜w10 は繁殖+0・兵は上限に張り付き・`fertile` が46体余ったまま、
掘っても湧かない飽和状態になった。**掘る判断そのものがゲーム**なのにその余地が消えていた。
→ 繁殖を専用枠に分離（`fromBreed` フラグ）。**プレイヤーの意思決定（掘削）と自動処理（繁殖）は枠を分ける。**

### ③ `fertile` は供給と消費が非対称
`fertile` は**繁殖しないと消えない**のに、勇者1体の出現ごとに最大6体ぶん付与される。繁殖が止まると溜まり続け、
`fertile` は城を優先するので**途中から兵が指令にもランダムウォークにも従わなくなった**（実プレイで発覚）。
→ **各種2体（繁殖に必要な数）までに制限**（`colFertileNeed()`）。掘削時の付与は廃止。
**新しく `fertile` を扱うときは必ず供給と消費が釣り合うか確認する。**

---

## バランスの現状（実測・`node test/p5/harness.js index.html colosseum`）

| 形 | 開通門 | 指令 | 初期兵力 | 到達 wave |
|---|---|---|---|---|
| Lv1・1門・周り4マス | 1 | なし | 82 | 7 |
| Lv10・1門・周り4マス | 1 | なし | 83 | 18 |
| Lv20・1門・周り4マス | 1 | なし | 82 | **26** |
| Lv20・1門・周り6マス | 1 | なし | 100 | 29 |
| Lv20・4門・周り4マス | 4 | なし | 100 | 24 |
| Lv20・1門・周り4マス | 1 | 城 | 82 | 35 |
| **Lv20・1門・周り4マス（最適）** | 1 | 門との中間 | 82 | **59** |
| Lv20・4門・周り4マス | 4 | 城 | 99 | 31 |
| Lv20・4門・周り4マス | 4 | 門との中間 | 97 | 21 |

- **💎ゼロ（Lv上限20）の幅は「素直にやって wave 26／最適プレイで wave 59」**
- 1門なら掘り広げるほど強い（26→29）が、4門開けると弱くなる（26→24）
- **指令が最も効く**（26→59）。ただし効く指令が状況で変わる（1門は前線に張る／4門は城に固める）
- 同時モンスターは最大101＝性能予算（80〜100体）の上端

数値ノブは `index.html` 先頭の「コロシアムモード」定数ブロックに集約。最重要は `COL_BREED_POP`（繁殖の専用枠）。

---

## 次にやること

### A. コロシアムの未検証点（優先）
1. **ゴールド制約下の序盤の手触り** — 実走テストは掘削コストを潤沢（`gold=100000`）にして測っているので、
   **開始20Gからの資金繰りは未検証**。開始20G＝20マス掘れる／wave1は勇者1体（Lv1: atk7/HP40）。
   きつすぎるなら開始ゴールドか討伐報酬（`15+wave`）で調整する。
2. **早送り時のコロシアム** — `COL_CASTLE_HP=150`（standard/hardは40）にしたので通常より余裕はある想定だが未実測。
3. **指令UIの操作感** — 🚩ボタン→盤面クリックの流れ、旗の視認性（30pxタイルに三角旗）、モバイルのタッチ。

### B. 前セッションからの持ち越し（未着手）
4. **hard モードの「連続流入」化** — 現行はどちらのモードも倒し切ってから次が湧く。hard を一定間隔で
   湧き続ける方式にする案。`epilogue_tempo` の `runFlood` で検証済み（Lv20ゴーレム8体なら捌ける／Lv10だと崩壊）。
   コロシアムが wave 制になったので**性格の住み分けはできている**。
5. **早送りで城が落ちる問題** — 早送りは勇者の攻撃レートも3.33倍になるため `CASTLE_MAX_HP=40` は
   Lv20勇者で約0.4秒。**対策案 40→120**。ユーザー判断で保留中（コロシアムだけ150にした）。
6. **宝具の時価売却**（Phase 2 の残り。宝具の出口が装備と鍛冶しかない）
7. **道B オンライン市場**（要バックエンド）
8. [宝具リスト.md](宝具リスト.md) への名前追加 → `TREASURE_NAMES` 反映（主要4系統 blade/shield/orb/relic は各ランクに残す）

---

## 検証の作法（触る前に読む）

```sh
test/p5/verify.sh index.html          # path/sim の黄金ハッシュ照合（コミット前に必ず）
node test/p5/harness.js index.html <mode>
```

| mode | 用途 |
|---|---|
| `colosseum` | **69項目**。盤面／城の配置と開通門／指令／wave／経済／精算／兵站／他モード非破壊 ＋ 実走 |
| `affix` | `dynamicEquip`＝ラン中の装備変更が即反映、`dynamicLevel`＝Lv上げの即反映。**強化を触ったらここ** |
| `balance` / `economy` / `tempo` | 戦闘・経済・テンポのものさし |
| `design` | 描画の性能予算（グラデ・`shadowBlur` 禁止）。**canvas描画やCSSを足したらここ** |
| `path` / `sim` / `pause` / `perf` | 経路探索・全軌道の等価性 |
| `trade` / `camera` / `forge` / `kind` | 取引コード／ミニマップ・追尾／鍛冶／宝具の系統 |

**黄金ハッシュ `path: a72aec6f` は不変であるべき**（経路ロジックを壊していない証明）。
`sim: 10dd6d78` / rngCount 462 はバランスを変えると当然変わるが、**今回は不変を維持できた**
＝standard/hard の挙動は1ビットも変えていない。

### 設計上の不変条件（崩さない）
- 描画は「地色＋暗部＋明部」の2階調ベタ塗り。**毎フレームのグラデ生成と `ctx.shadowBlur` は禁止**（D-2/D-3）
- 経路探索は距離場O(1)＋ホライズンBFS。**深度非依存**を壊さない
- 強化（Lv・宝具）は**常に即反映**。ちびは成体の半分、HPは割合維持
- モードの性格: standard=ゴールドを稼ぎやすい・ダイヤは稼ぎづらい・低難度／hard=リスクありダイヤ稼ぎやすい・
  高難度で放置不可／**コロシアム=腕前の記録**。standard のダイヤ率を上げるのは意図に反する（過去に却下済み）
