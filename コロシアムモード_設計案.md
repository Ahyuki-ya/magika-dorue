# コロシアムモード 設計案

standard / hard に続く第3のモード。**スクロールなし・固定盤面・何 wave 耐えられるか**のスコアアタック。

---

## 0. このモードの一行

> **有限の壁を、いつ兵に換えるか。掘り広げれば兵は増えるが、繋げた門からも勇者が来る。**

現行の掘削は「1G で壁を壊す → その種類のモンスターが1体湧く」（`dig()`）。盤面を固定した瞬間、**壁 = 未来の兵の在庫**になり、掘る判断そのものがゲームになる。standard / hard では無限に下へ掘れるためこの緊張は存在しない。

そこに**攻め口の管理**が乗る。四方の壁には門が1マスずつ空いているが、内側は全部が壁。**城に到達できる門からしか勇者は来ない**ので、最初は掘り抜いた1方向だけが攻め口になる。兵力が欲しくて掘り広げ、別の門に届いてしまうと、そこも攻め口になる。

だから主題は3つが同時に回る：**迷路づくり**（通路の形が勇者の道になる）／**有限資源の配分**（壁＝兵の在庫）／**攻め口の管理**（開けた門の数だけ守る方向が増える）。

---

## 1. 盤面

### 形状
**21 × 21**（外周1マスは破壊不能の闘技場壁）→ canvas 630 × 630px。スクロールなしで全体が見える。

開始時は**内側が全部壁**で、穴は四方の門1マスずつだけ。通路はプレイヤーが掘って作る。

```
       開始時                          プレイした後
■■■■■門■■■■■              ■■■■■門■■■■■
■□□□□□□□□□■              ■□□□□・□□□□■     ■ 外壁（破壊不能）
■□□□□□□□□□■              ■□□・・・□□□□■     □ 未掘削の壁（＝兵の在庫）
門□□□□□□□□□門     →       門□□・□□□□□□門     ・ 掘った通路（＝勇者の道）
■□□□□□□□□□■              ■□□・🏰・□□□□■     🏰 自分で置いた城
■□□□□□□□□□■              ■□□□・□□□□□■
■■■■■門■■■■■              ■■■■■門■■■■■
                                   ↑ 北の門だけが開通。他の3門からは来ない
```

- **門は四辺の中央4カ所**。穴は**その1マスだけ**で、内側は全部壁
- **城はプレイヤーが置く**。どれかの門から掘り進んだ場所にのみ配置でき、**置いた瞬間に第1波の準備が始まる**
- **城に到達できる門からしか勇者は来ない**（`colOpenGates()`）。最初は掘り抜いた1方向だけ
- 掘れる壁 = **361マス**（内側の全マス）
- 城の移動（50G）も可能。移動先も門から到達可能である必要がある

### 鉱石分布 — 「深度」を「中心からの距離」に読み替える
中心ほど良質にする。理由：追い詰められて中心を掘ると強いゴーレムが出る＝**最後の切り札**になり、同時に**城の周りが空洞化して敵が殺到しやすくなる**というリスクがセットになる。

チェビシェフ距離 `r = max(|x−10|, |y−10|)`

| 帯 | マス | 銅(ゴーレム) | 苔(ゴブリン) | 石(スライム) |
|---|---|---|---|---|
| r1–3（内周） | 48 | 40% | 30% | 30% |
| r4–6（中周） | 120 | 20% | 25% | 55% |
| r7–9（外周） | 192 | 5% | 20% | 75% |
| **合計** | **361** | ≈53 | ≈88 | ≈220 |

盤面の中心は門から最も遠い＝掘り進むのに手間がかかる場所なので、「奥ほど良い資源」という意味になる。城を中心寄りに置けば良い鉱石に手が届くが、掘り広げると四方の門すべてに近づく。

- **闇水晶（レイス）**: ショップ解禁時のみ r1–2 に 15%（約4マス）で上書き
- **宝石鉱脈**: **固定4個**を r5–8 のランダム位置に配置。可視なので「取りに行く」目標になる（＝宝具4個確定）

---

## 2. wave

| 項目 | 仕様 |
|---|---|
| 体数 | `n(N) = min(8, 1 + floor((N−1)/3))` → w1:1 / w4:2 / w7:3 … w22:8 |
| 勇者Lv | **全員 Lv = N**（wave 番号だけで決まる） |
| ボス | 10の倍数 wave は怒り勇者1体＋通常 n−1 体 |
| 門 | **開通している門からランダムに選ぶ**。同時に使う数は w1–6 で1 / w7–15 で最大2 / w16+ で最大4 だが、**開通数で頭打ち**（1門しか開けていなければ終盤でもそこからだけ） |
| 準備時間 | 全滅後 **8秒**（早送り3秒）。スキップで早出しボーナス `残り秒 × 5G` |
| 予告 | `⚠ Wave 12 — 北・東の門 5体（ボス1）… 8s` ＋ 該当門に矢印 |

**準備時間の意味**が明確なのが要点：予告された門の方向の壁を掘って兵を出す → Lv上げ → 城修復 → 繁殖待ち。

### 勇者の強さ（現行の式そのまま・富スケールは無効）
`heroPower(N) ≈ 20 + 1.3N`、10wave ごとに rageBonus +5/各ステータス、HP は `×HERO_HP_MULT(5)`。

| wave | atk | agi | HP(1体) | 体数 | 総HP | 総DPS |
|---|---|---|---|---|---|---|
| 10 | 16 | 16 | 80 | 4 | 320 | 104 |
| 20 | 25 | 25 | 130 | 7 | 910 | 283 |
| 30 | 34 | 35 | 175 | 8 | 1,400 | 440 |
| 50 | 53 | 53 | 270 | 8 | 2,160 | 688 |

**hard の「富スケール（`heroWealth`）」はコロシアムでは無効**。難易度カーブを wave だけで決めることで、記録が比較可能になる。

---

## 3. 兵站 — 完全有限＋繁殖のみ

### 掘削
361マスが総在庫。掘り切ったら新規召喚は不可能。

### 繁殖（唯一の再生産）
現行コードの性質をそのまま使う：

- 繁殖には**城の3×3以内に同種の fertile 成体が2体**必要
- 繁殖すると**親2体の fertile が消える**（消費型）
- fertile の再供給は**勇者1体の出現ごとに最大6体ぶん**（スライム2・ゴブリン2・ランダム1種2）

→ **繁殖レートが勇者の出現数に自動連動する**。wave が進むほど補充も増える自己調整ループ。完全有限モードの収入としてそのまま機能する。

### 変更が要る4点
| 変更 | 理由 |
|---|---|
| **指令（種別ごとの集合地点）を追加** | §3' 参照。掘った迷路の要所に兵を張れる |
| **`fertile` は各種2体まで**（`colFertileNeed()`） | 繁殖に必要なのは2体。それ以上付けると指令に従わない個体が増え続ける（§3' の失敗②） |
| **掘削では `fertile` を付けない** | 同上。補充は勇者の登場時（`markFertile`）だけに一本化 |
| **軍の同時上限 `COL_ARMY_CAP = 100`** | 性能予算（80〜100体）の上端。掘れる量の物理的な天井 |
| **繁殖の専用枠 `COL_BREED_POP = 12`** | **掘削とは別枠**。ここを間違えると掘る楽しさが消える（下記） |

#### 実測で崩れた前提①：繁殖のボトルネック
設計時は「城の隣接8マスが繁殖のボトルネックになる」と見込んでいたが、**エンティティは同じマスに重なれる**ため、これはボトルネックにならない。そこへ当時の設定（掘削時の fertile 50%・全個体が城へ集まる。どちらも後に廃止）が重なると繁殖判定が常時通り、実測で**同時240〜431体**まで膨れ、**Lv10でも40wave完走・城無傷**になった。

#### 実測で崩れた前提②：枠を共通にすると「掘る楽しさ」が死ぬ（最重要）
対策として最初は「掘削と繁殖で共通の軍上限60」を入れたが、**これがこのモードの芯を殺した**。
繁殖は毎フレーム自動判定されるので、戦闘で枠が空いた瞬間に繁殖が先に埋めてしまう。実測すると：

| wave | 繁殖 | 兵 |
|---|---|---|
| w1〜w10 | **+0** | 60（上限に張り付き） |
| w20 | +4 | 60 |

`fertile` 成体が **46体も余ったまま**、掘っても「上限です」で湧かない飽和状態。**掘る判断そのものがゲーム**なのに、掘る余地が消えていた。

→ **繁殖を専用枠にして、掘削の枠を食わないようにした**（`fromBreed` フラグで繁殖由来を数え、`COL_BREED_POP=12` で頭打ち）。これで総兵力67〜75、上限100まで25〜33体ぶんの掘る余地が常に残る。

**この修正で「掘り広げると強くなる」に逆転した**（下表：1門なら r4 の 26 → r6 の 29）。以前 r6 が弱かったのは、上限に張り付いて掘っても兵が増えず、空洞だけが広がっていたため。枠を分けた今は掘った分がそのまま兵になる。

種別の役割が自然に分かれる：探知距離が ゴブリン10 / レイス12 に対し スライム4・ゴーレム4 なので、**ゴブリン＝遊撃、ゴーレム＝城壁**になる。

HUD に `⚔ 兵 67/100　🥚 繁殖 12/12` を出して、掘るかどうかの判断材料にしている。

---

## 3'. 指令（種別ごとの集合地点）

**掘った迷路に意味を持たせるための仕組み。** 強化パネルの 🚩 で種別を選び、盤面の通路をクリックして集合地点を決める。

| 状態 | 挙動 |
|---|---|
| 指令なし（既定） | **カオスにランダムウォーク**（通常モードと同じ。4マス先を見て通路が長い方向に重み付け） |
| 指令あり | 指定地点の**3×3で待機**。`range` 内に敵が入れば従来どおり追う |
| `fertile` な個体 | **指令より繁殖（城の3×3）を優先**。繁殖条件がその範囲なので。ただし `fertile` は**各種2体まで**（`colFertileNeed()`）＝城へ行くのは常に最大6体 |

`fertile` が城を優先するので、**繁殖に回る個体と前線に張る個体が自動で分かれる**。
種別ごとに指定できるので「ゴブリンは入口、ゴーレムは城の前」のような陣形が組める。同じ地点をもう一度クリックすると解除（＝ランダムウォークに戻る）。

### 失敗①：「全員が城へ集まる」実装
四方から包囲される想定で、コロシアムでは fertile でない個体も強制的に城へ向かわせていた。しかし**掘った通路の形に意味がなくなった**（湧いた兵がすぐ城へ歩いて行き、迷路の途中で迎撃しない。戦場が城の周囲だけになる）。「迷路を作る楽しさ」を殺していたので、指令機能に置き換えた。

### 失敗②：`fertile` が溜まって指令が効かなくなる（実プレイで発覚）
指令を入れた直後、**途中から兵が指令にもランダムウォークにも従わなくなる**という報告があった。原因は `fertile` の供給と消費の非対称：

- 勇者が1体湧くごとに `markFertile()` が最大6体（スライム2・ゴブリン2・ランダム1種2）に付与する
- `fertile` な個体は**指令より繁殖（城）を優先**する
- しかし **`fertile` は繁殖しないと消えない**。繁殖枠12が埋まると繁殖が止まり、`fertile` が溜まる
- コロシアムは wave で複数体湧くので `6 × 体数` ずつ増える → 城の3×3で「待機」する個体が際限なく増える

→ **`fertile` を各種2体（繁殖に必要な数）までに制限**（`colFertileNeed()`）。掘削時の付与も廃止し、補充は勇者の登場時だけに一本化した。これで城へ行くのは常に各種2体で、残りは指令かランダムウォークに従う。

### 指令の効果（実測）
| 形 | 指令なし | 指令＝城に集める | 指令＝門との中間に張る |
|---|---|---|---|
| **1門**・周り4マス | 26 | 35 | **59** |
| **4門**・周り4マス | 24 | **31** | 21 |

- **1門なら「通路の途中で迎撃」が圧倒的**（26 → **59**）。一本道に兵を張るのがタワーディフェンスとして正しい形
- **4門なら「城に固める」**（24 → 31）。4方向に分散させると各方面が薄くなって逆に弱い（21）
- **状況によって最適な指令が変わる**＝指令が戦術判断として機能している。指令なしとの差は2倍以上あるので、操作の価値も大きい

---

## 4. 戦力の天井（バランスの背骨）

`mstatsAt()` の実測値（HPは `MONSTER_HP_MULT=4` 込み、行動レートは `ATK_UNITS_CAP=9` 込み）：

| ゴーレム | atk | HP | DPS |
|---|---|---|---|
| Lv1 | 2 | 16 | 1.4 |
| Lv10 | 7 | 68 | 10.1 |
| Lv20 | 12 | 124 | 19.4 |

### 実走の結果（`node test/p5/harness.js index.html colosseum`）
軍の同時上限を入れたうえで、城の周囲を掘って実際に wave を回した実測：

| 形 | 開通門 | 指令 | 初期兵力 | 到達 wave |
|---|---|---|---|---|
| Lv1・1門・周り4マス | 1 | なし | 82 | **7** |
| Lv10・1門・周り4マス | 1 | なし | 83 | **18** |
| Lv20・1門・周り4マス | 1 | なし | 82 | **26** |
| Lv20・1門・周り**6**マス | 1 | なし | 100 | **29** |
| Lv20・**4門**・周り4マス | 4 | なし | 100 | **24** |
| Lv20・**4門**・周り**6**マス | 4 | なし | 100 | **23** |
| Lv20・1門・周り4マス | 1 | 城 | 82 | **35** |
| **Lv20・1門・周り4マス（最適プレイ）** | 1 | 門との中間 | 82 | **59** |
| Lv20・**4門**・周り4マス | 4 | 城 | 99 | **31** |
| Lv20・**4門**・周り4マス | 4 | 門との中間 | 97 | **21** |

読み取れること：

1. **育成の差がそのまま出る** — Lv1で7 → Lv10で18 → Lv20で26（指令なし）。
2. **1門なら掘り広げるほど強い**（26 → 29）。壁を兵に換えた分がそのまま戦力になる。
3. **4門開けると弱くなる**（26 → 24）。攻め口が増えるぶん守りが薄くなる。
4. **指令が最も効く**（26 → 59）。ただし効く指令は状況で変わる（§3' 参照）。

→ **💎ゼロ（Lv上限20）の幅は「素直にやって wave 26／最適プレイで wave 59」。**
「どこに城を置き、どの門を開け、どこに兵を張るか」がそのまま戦略になっている。

同時モンスターは最大101＝性能予算（80〜100体）の上端。

`balance-model` にある「上限20で波100」は現行の1体ずつ方式の数字。コロシアムは同時8体なので別の目盛りになる。

---

## 5. 経済

**実レベルは Lv1 スタート（ラン内で育成）**。上限（💎解放ぶん）・ショップ・装備宝具は反映する。これで記録がほぼ公平になり、メタ進行の価値も残る。

| ソース / シンク | 値 |
|---|---|
| 開始ゴールド | 20 ＋ ショップ「初期ゴールド増加」 |
| 討伐報酬 | **15 + wave** G（w10:25 / w30:45 / w50:65） |
| 早出しボーナス | 準備時間の残り秒 × 5G |
| シンク①レベル上げ | 3種 Lv20 まで **3,966G** ／ Lv50 まで 27,816G |
| シンク②掘削 | 1G × 最大361 |
| シンク③城の修復 | **100G で全回復**（wave間のみ） |

実測：wave26（指令なしの Lv20）までの累計討伐は約 **130体**、報酬合計は約 **3,700G** ⇔ 3種Lv20の **3,966G**。ほぼ均衡でゴールドが余らない。最適プレイ（wave59）まで伸ばせば約 **17,000G** 稼げるので、💎で上限を上げた先の投資先になる。
💎で上限を上げると Lv上げコストが跳ね上がる（Lv50 まで1種9,272G）ので、**上限解放が「もっと wave を伸ばせ」という圧力になる**。

**ゴールドは銀行に持ち帰らない。** standard の「ゴールドを稼ぐモード」という役割と競合させないため。

### 城
- `COL_CASTLE_HP = 150`（standard/hard は40。早送りでも迎撃の猶予が残る＝持ち越し事項#2の回避）
- wave クリアごとに **+20 自然回復**、100G で全回復

---

## 6. メタ報酬

**最高記録を更新したときだけ💎を配る。**

- 到達 wave `W` に対する累計付与 = `floor(W / 5)` 💎、記録更新で差分のみ支給
- wave30 → 累計6💎 / wave50 → 10💎 / wave100 → 20💎

hard = 回すほど貯まる「量」、コロシアム = 腕前の「記録」。💎の総需要155個（ショップ107＋上限解放48）に対して補助的な供給に収まる。

宝石鉱脈の宝具4個は通常どおり持ち帰る。

記録は `magika_colosseum_best` に保存し、モード選択画面に「最高 Wave XX」を表示。

---

## 7. 無効化するもの / 新規

### 無効化
| 要素 | 理由 |
|---|---|
| レバレッジ・銀行・預入 | コロシアムは参加条件なし。誰でも触れる第2のモード |
| 勇者の富スケール | 難易度を wave だけで決める（記録の比較可能性） |
| 勇者の家（前線基地） | 盤面が狭く即詰み（`HERO_HOUSE_GAP=30` が深度17の盤面では自然に発火しない） |
| 掘削の距離制限 | `DIG_RANGE_BELOW_CASTLE` は「下へ潜る」ための制限なので適用しない |
| ミニマップ | 全面が見えている |
| `expandMap()` | 盤面は増えない |
| 撤退・利確 | 耐えるだけ |

### UI
- 深度UI → **Wave UI**（現在 wave / 最高記録）
- `⚔ 兵 67/100　🥚 繁殖 12/12` ／ `🚪 開通した門 2/4（北・東）` — 掘るかどうかの判断材料
- 強化パネルの各種族に **🚩 指令ボタン**（未設定／`🚩10,3` のように座標表示）。盤面には種別色の旗
- 次wave予告（門・体数・ボス有無・残り秒）＋ 盤面の門に矢印
- あくま部屋ボタンは通常モードと同じ（作成／移動）。**早出し**と**修復**は専用ボタン
- リザルト: 「Wave XX 到達」／記録更新の表示／獲得💎

---

## 8. 実装上の注意

| 項目 | 内容 |
|---|---|
| **`COLS` を `let` にする** | 唯一の構造的変更（`const COLS = 20` → `let COLS`、`BASE_COLS` を新設）。参照側は全て実行時に読むので他の修正は不要だった。canvas と `#canvasContainer` の幅・高さはモード切替時にインラインで設定 |
| **`isReachable` の空判定** | `ny >= SKY_LAYERS` が北の門(y=0)を弾いていた（実装中に実測で発覚）。`skyLimit()` に置換。standard では値が同じなので挙動不変 |
| 経路探索は触らない | `verify.sh` の**黄金ハッシュ `a72aec6f` は不変**（実装後も一致を確認）。`sim` の `10dd6d78`/462 も不変＝standard/hard の挙動は1ビットも変わっていない |
| 描画方針 | 外壁は既存のタイル3（入口天井）を再利用した（新規プリレンダ不要）。門の予告矢印は `fillStyle`+`fill` のみ。`ctx.shadowBlur` と毎フレームのグラデ生成は禁止（D-2/D-3） |
| 同時体数 | `COL_ARMY_CAP = 100` で頭打ち。実測の最大101 |
| 検証 | `test/p5/epilogue_colosseum.js`（`... colosseum`）。**69項目 PASS**。盤面・wave構成・経済・精算・兵站・他モード非破壊、そして wave別の物量と到達waveの実走。**1'a〜1'i が「開通した門＝攻め口」、5g〜5r が指令（5q/5r は fertile 上限の回帰テスト）、5b〜5d が「繁殖が掘削の枠を食わない」の回帰テスト** |

---

## 9. 数値ノブ一覧（調整用）

すべて `index.html` 先頭の「コロシアムモード」定数ブロックに集約。

| 定数 | 採用値 | 効果 |
|---|---|---|
| `COL_SIZE` | 21 | 盤面。掘れる壁の総数（361）＝兵の総在庫 |
| `COL_ARMY_CAP` | **100** | 軍の同時上限（性能予算の上端）。掘れる量の天井 |
| `COL_BREED_POP` | **12** | **最重要ノブ**。繁殖由来の生存数の上限。掘削とは別枠。ここを共通枠にすると掘る楽しさが消える |
| `COL_BREED_MAX` | **3** | 種別ごとのちび同時数。繁殖の瞬発力を抑える |
| `colWaveHeroCount` | `1+floor((N−1)/3)` cap 8 | 同時体数。難易度の主軸 |
| `COL_PREP_SEC` | 8（早送り3） | テンポと準備の余裕 |
| 討伐報酬 | `15 + wave` | Lv上げ速度。累計がLv上げ総コストと均衡するよう設定 |
| `COL_CASTLE_HP` | 150 | 陥落までの猶予（+`COL_CASTLE_HEAL`=20/wave、`COL_REPAIR_COST`=100G） |
| `colFertileNeed()` | 各種2体まで | fertile の上限。増やすと指令に従わない個体が増える |
| `COL_DIAMOND_PER` | 5 wave / 1💎 | メタ報酬 |
| `COL_BANDS` | 上表 | 中心の価値＝切り札の強さ |
| `colRally` | 種別ごと・既定なし | 指令（集合地点）。未設定＝ランダムウォーク。`fertile` は城優先 |
