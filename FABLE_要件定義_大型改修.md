# Magika・Dorue 大型改修 要件定義（fable 引き継ぎ用ドラフト）

> このドキュメントは次セッション（Claude Fable）へ渡すための**要件の叩き台**です。
> 実装はまだ行いません。設計方針の合意形成が目的です。確定した項目は CLAUDE.md へ移します。

---

## 改修テーマ

1. **ハードモードの「レバレッジ」化** — リスクとリターンを対称に増幅する高難度体験
2. **レア度付きアイテム（宝物）システム** — 収集・強化の縦軸
3. **プレイヤー間の出品／取引システム** — 収集物を他プレイヤーと売買する横軸

3つは独立にも実装できるが、「レバレッジで稼ぐ → レア宝物を掘り当てる → 出品して他プレイヤーと取引」という一本の動線として設計すると噛み合う。

---

## 1. ハードモードのレバレッジ化

### コンセプト
現状ハードは「報酬が可変・怒り勇者ボーナスなし」という*渋い*調整。これを「**倍率を賭ける**」方向に作り替え、ハイリスク・ハイリターンにする。

### 設計案：レバレッジ倍率（賭け率）
- ゲーム開始時、またはラン中に **レバレッジ倍率 L**（例: 1x / 2x / 3x / 5x）を選択
- **報酬**（勇者撃破ゴールド・ダイヤドロップ率・宝物ドロップ率）が L 倍
- **損失**も L 倍：
  - あくま部屋の被ダメージが L 倍（`castleHp` の減りが速い）
  - もしくは「掘削コスト・強化コストが L 倍」
  - もしくは「ゲームオーバー時、引き継ぎゴールドから L に応じた没収」
- どこにリスクを乗せるかは要検討（下記トレードオフ参照）

### リスクの乗せ先 3案

| 案 | リスクの内容 | 長所 | 短所 |
|---|---|---|---|
| **A: 被ダメ倍率** | 城のHP減少が L 倍速い | 直感的、ラン中に緊張感 | 序盤事故が増えすぎる恐れ |
| **B: コスト倍率** | 掘削・強化が L 倍高い | 経済的な駆け引きになる | 地味、体感リスクが薄い |
| **C: 没収** | 敗北時に引き継ぎ資産を L 比例で没収 | 「賭け」の感覚が最も強い | 周回意欲を削ぐ劇薬。要慎重 |

**推奨:** A（被ダメ倍率）を主軸に、C（没収）は控えめに併用。倍率選択のたびに「期待値」を UI で明示すると納得感が出る。

### 実装インパクト
- `let leverage = 1;` を追加、モード選択画面に倍率ピッカーを追加
- 報酬計算（`updateEntity` 内の reward / ダイヤドロップ）に `* leverage`
- 損失側の計算（castleHp 減算 or コスト）に `* leverage`
- リザルトに「賭け率」「純益（倍率込み）」を表示
- localStorage: 特に新規キー不要（ラン限定パラメータ）。没収案を採る場合のみ引き継ぎ処理に手を入れる

### 検討事項（fable への質問）
- リスクの乗せ先は A / B / C のどれか（複合か）
- 倍率の刻み（1/2/3/5? それとも連続スライダー?）
- ラン開始時固定か、ラン中に変更可能か

---

## 2. レア度付きアイテム（宝物）システム

### コンセプト
宝石鉱脈（既存タイル8）や勇者撃破の延長として、**レアリティを持つ「宝物（Treasure）」**をドロップさせる。プレイヤーはこれを収集し、装備効果を得たり、出品して取引する。

### レアリティ階層（案）
| ランク | 色 | ドロップ重み | 例 |
|---|---|---|---|
| Common（並） | 灰 `#95a5a6` | 60% | 錆びた剣、欠けた石 |
| Uncommon（良） | 緑 `#2ecc71` | 25% | 銅の護符 |
| Rare（稀） | 青 `#3498db` | 10% | 苔むした王笏 |
| Epic（極） | 紫 `#9b59b6` | 4% | 深淵のクリスタル |
| Legendary（伝説） | 金 `#ffcc00` | 1% | 魔王の宝冠 |

- レアリティ重みは**深度・レバレッジ倍率・撃破勇者レベル**で上方シフト（深いほど・賭けが高いほど良い物が出る）
- 宝物には**ランダムな効果**（affix）を付与できる（例: 「初期ゴールド +X」「slime攻撃 +Y」「繁殖上限 +1」）→ ローグライク的な引きの楽しさ

### データ構造（案）
```javascript
// 1つの宝物インスタンス
{
  id: "tz_<uuid>",          // 一意ID（取引・重複防止に必須）
  name: "深淵のクリスタル",
  rarity: "epic",           // common|uncommon|rare|epic|legendary
  icon: "💎",
  affixes: [                // 付与効果（0〜3個、レア度で数が増える）
    { stat: "startGold", value: 30 },
    { stat: "slimeAtk",  value: 2 }
  ],
  bornDepth: 152,           // 出土深度（フレーバー＆真贋の材料）
  bornRun: 47,              // 何周目で出たか
  createdAt: 1719900000000
}
```

### 保管・装備
- localStorage: `magika_inventory`（宝物配列）
- 「その他設定 → 宝物庫（Treasury）」画面で一覧・ソート・装備管理
- 装備スロット（例: 3枠）を用意し、装備中の宝物の affix がランに適用される
- ショップの恒久強化とは別系統（宝物はラン跨ぎで付け替え可能な「装備」）

### 実装インパクト
- 宝物生成器 `rollTreasure(depth, leverage, heroLv)`
- インベントリ画面（新規サブスクリーン）
- 装備適用処理を `enterGameScreen` の初期化に追加（`shopData` 適用の隣）
- 宝物ドロップ演出（レア度別のパーティクル色・SE・専用ファンファーレ）

### 検討事項
- affix の種類とバランス（強くしすぎると恒久強化が陳腐化）
- 装備スロット数、レア度による装備制限の有無
- 分解 → 素材 → 合成のクラフト要素まで踏み込むか（スコープ拡大注意）

---

## 3. プレイヤー間の出品／取引システム

### 最重要の前提整理
現行アーキテクチャは **単一HTML・サーバーレス・localStorage のみ**（CLAUDE.md の "No build tools, no dependencies, no server required"）。
**真の「オンライン取引」は、この前提と根本的に衝突する。** 他プレイヤーとデータを共有するには、必ず何らかの共有バックエンド（サーバー or 外部サービス）が要る。

そこで **2つの道**を提示する。どちらを選ぶかで実装難度・運用コスト・体験が大きく変わる。

---

### 道 A: サーバーレス擬似取引（現行思想を維持）

共有DBを持たず、**取引を「コード（文字列）の受け渡し」で成立**させる。

- **出品 = エクスポート**: 宝物を署名付き文字列（Base64+チェックサム）にエンコードして「取引コード」を発行。プレイヤーはこれを SNS 等で共有
- **入手 = インポート**: 他人の取引コードを貼り付けると、検証後に自分のインベントリへ追加
- **重複・複製対策**:
  - 各宝物に一意 `id` を持たせ、インポート済み id を localStorage で記録して二重取得を拒否
  - ただし「コードを配った側の手元にも残る」問題（＝複製）は原理的に防げない
  - → **贈与モデル**（コピー配布）と割り切るか、**ローカル焼却**（出品するとエクスポート元では使用不可フラグを立てる）で緩和

| 長所 | 短所 |
|---|---|
| サーバー不要・無料・現行思想を維持 | 真の「市場」は作れない（価格・入札なし） |
| GitHub Pages のまま公開可能 | 複製を完全には防げない（性善説 or 焼却で緩和） |
| 実装が単一HTMLで完結 | 取引は手動のコード交換に留まる |

**GitHub Pages 公開との相性: ◎**（追加コストゼロ）

---

### 道 B: 本格オンライン市場（バックエンド導入）

共有バックエンドを立て、**出品・購入・入札**が全プレイヤー間で成立する真のマーケット。

- **必要な構成要素**:
  - 認証（匿名ID or メール/OAuth）
  - 出品DB（誰が・何を・いくらで）
  - 取引の原子性（二重売買防止＝サーバー側ロック）
  - 通貨の権威的管理（ダイヤ残高をサーバーが保持。でないとチート自明）
- **選択肢**（運用コスト順）:
  1. **BaaS（Firebase / Supabase）** — 無料枠あり、実装が最速。推奨の第一候補
  2. **軽量自前API**（Cloudflare Workers + KV/D1 など）— 低コスト、学習コストあり
  3. **本格サーバー**（Node + DB）— フルコントロールだが運用重い

| 長所 | 短所 |
|---|---|
| 価格・入札・在庫のある本物の市場 | サーバー運用コスト・保守が発生 |
| チート耐性（サーバーが権威） | 単一HTML/サーバーレス思想を放棄することになる |
| ランキング等へ拡張しやすい | 認証・不正対策・スパム対策が必須になる |

**GitHub Pages 公開との相性: △**（静的ホスティングは維持できるが、別途 API/DB のホスティングが必要）

---

### 重大な設計上の警告（fable が必ず考慮すべき点）

真の取引経済を入れる場合、以下は**避けて通れない**：

1. **チート/改ざん**: localStorage は誰でも書き換え可能。「ダイヤ9999」「Legendary量産」を防ぐには、通貨と資産の権威をサーバーに置くしかない（＝道B必須）。道A（サーバーレス）でランキングや市場を作ると即崩壊する。
2. **複製（dupe）**: 道Aでは原理的に複製を防げない。「贈与」と割り切るか、焼却で緩和。
3. **不正コンテンツ/命名**: プレイヤーが宝物名などを自由入力できると、不適切文字列の共有経路になり得る。名称は固定テーブルから選ばせる等、自由入力を避ける設計が安全。
4. **未成年配慮とRMT**: ゲーム内資産の取引が現実マネー取引（RMT）に接続しないよう、換金性を持たせない。

### 推奨ロードマップ
1. **まず道A（コード交換）で「取引の体験」を最小コストで実装** → 単一HTML思想を保ったまま出品/入手のUXを検証
2. 需要と手応えを見て、**本気で市場化するなら道B（Supabase等のBaaS）へ移行** → その時点で通貨をサーバー権威に移し、チート対策を設計

### 検討事項（fable への質問）
- サーバーレス（道A）維持か、バックエンド導入（道B）を許容するか
- 取引モデルは「贈与（コピー）」か「焼却付き譲渡」か「価格付き売買」か
- 通貨は既存ダイヤを使うか、取引専用通貨を新設するか
- GitHub Pages 単独公開を続ける必要があるか（道Bは別ホスティングが要る）

---

## 全体のデータ／localStorage 影響まとめ

| キー | 状態 | 用途 |
|---|---|---|
| `magika_carryover` | 既存 | 引き継ぎゴールド |
| `magika_diamond` | 既存 | 引き継ぎダイヤ |
| `magika_monsterlevels` | 既存 | モンスターLv |
| `magika_runcount` | 既存 | 周回数 |
| `magika_shop` | 既存 | 恒久強化 |
| `magika_records` | 既存 | 実績・記録 |
| `magika_mute` | 既存 | サウンド設定 |
| `magika_inventory` | **新規** | 所持する宝物配列 |
| `magika_equipped` | **新規** | 装備中の宝物ID |
| `magika_trade_seen` | **新規（道A）** | インポート済み宝物IDの重複防止 |
| （サーバーDB） | **新規（道B）** | 出品・残高・認証 |

## 段階的な実装順序（推奨）
1. **レバレッジ**（ラン内で完結・最小リスク・体験の核）
2. **宝物ドロップ + インベントリ + 装備**（収集の縦軸・ローグライク性強化）
3. **道A の取引コード**（サーバーレスで出品体験を検証）
4. （必要なら）**道B のオンライン市場**（大規模改修・別途合意）
